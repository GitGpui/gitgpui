# Performance Optimization Task List

Fresh run reset on March 24, 2026.

This file is intentionally cleared. Recreate tasks only after the suite has been rebaselined with the new allocation metrics.

## Rebaseline Checklist

- [ ] Run the main Criterion suite
- [ ] Run `app_launch/*` on a compositor-capable runner/session that permits local Wayland/X11/DBus socket IPC
- [ ] Run `idle/*`
- [ ] Run `perf_budget_report --skip-missing`
- [ ] Record the first timing outliers
- [ ] Record the first allocation outliers

## Current Rebaseline Blocker (March 25, 2026)

- `app_launch/*` remains blocked on this runner. `perf-app-launch` now fails at harness preflight, before child launch, with an environment blocker even though `DISPLAY=:0`, `WAYLAND_DISPLAY=wayland-0`, `XDG_RUNTIME_DIR=/run/user/1000`, and `XAUTHORITY=/run/user/1000/.mutter-Xwaylandauth.HT10M3` are present.
- A direct March 25 rerun of all six `app_launch/*` scenarios now fails identically at that preflight with exit `3`, before fixture setup or child spawn, so repeating different `app_launch/*` cases on this runner will not produce fresh timing or allocation data until the environment changes.
- Direct Unix-socket probes now confirm this is not just a missing env-var issue: `nc -U /run/user/1000/wayland-0` and `nc -U /tmp/.X11-unix/X0` both fail with `Operation not permitted`, and `dbus-run-session` cannot bind its own local socket under `/tmp` or `/run/user/1000`. This sandboxed runner is denying the local socket IPC that GPUI, X11, and Wayland need.
- A further March 25 environment check confirmed the restriction is broader than those display endpoints: `xdpyinfo` cannot open `DISPLAY=:0`, Python cannot even create or bind fresh AF_UNIX / AF_INET sockets (`socket()` / `bind()` return `Operation not permitted`), abstract Unix sockets fail the same way, and `Xvfb` cannot open `inet`, `inet6`, or `local` listeners. There is no credible local socket-based workaround for `app_launch/*` inside this sandbox.
- GPUI headless mode is not a credible `app_launch/*` fallback on this platform. The Linux/FreeBSD headless client cannot open the main window, while the startup probe records `first_paint` / `first_interactive` from real window prepaint/next-frame callbacks.
- A March 25 reference-repo audit confirmed there is no hidden Linux offscreen launch path to reuse here. In `zed`, `gpui_linux::current_platform(true)` switches to `HeadlessClient`, `HeadlessClient::open_window()` still bails instead of creating a real window, and `gpui_platform::current_headless_renderer()` returns `None` on non-macOS. There is no Linux headless renderer path in the upstream GPUI stack that could credibly emit comparable `first_paint` / `first_interactive` launch milestones inside this sandbox.
- The tracked `criterion/app_launch/*/new/sidecar.json` artifacts are still the March 24, 2026 baseline (`04:58:05` through `04:58:21` local) and must not be treated as fresh March 25 rebaseline data on this runner.
- There are still no `target/criterion/app_launch/*/new/sidecar.json` artifacts on this checkout either. A fresh March 25, 2026 `cargo run --quiet -p gitcomet --bin perf-app-launch -- --preflight-only --bench app_launch/cold_empty_workspace` recheck still exits `3` with the same local Wayland/X11 `Operation not permitted` socket-probe failures, so the default local Criterion root still has no credible `app_launch/*` baseline.
- A broader March 25, 2026 audit under `/home/sampo` also found no newer `app_launch/*/new/sidecar.json` artifacts anywhere else on this machine. Do not spend more local turns searching this filesystem again unless a fresh external rerun is explicitly copied in.
- Those six tracked March 24 launch sidecars are also pre-allocation-schema artifacts: they only contain `first_paint_ms`, `first_interactive_ms`, RSS, and `repos_loaded`, with no `first_paint_*` / `first_interactive_*` allocation counters. Even if their timings were otherwise acceptable, they still cannot satisfy the current allocation-aware rebaseline.
- Do not check off `Run app_launch/*` until all six launch cases (`cold_empty_workspace`, `cold_single_repo`, `cold_five_repos`, `cold_twenty_repos`, `warm_single_repo`, `warm_twenty_repos`) have been rerun on a compositor-capable session, their `app_launch/*` sidecars under the active Criterion root (`target/criterion` by default for local reruns, or the path passed via `--criterion-root` / `GITCOMET_PERF_CRITERION_ROOT`) have fresh mtimes from that rerun, and each sidecar includes non-missing milestone allocation fields such as `first_paint_alloc_bytes`, `first_paint_alloc_ops`, `first_interactive_alloc_bytes`, and `first_interactive_alloc_ops` alongside `first_paint_ms`, `first_interactive_ms`, and `repos_loaded`.
- `perf_budget_report` now needs a freshness gate when stale `app_launch/*` sidecars remain on disk under the active Criterion root. Use `--fresh-reference PATH` (where `PATH` predates the fresh suite run) so the report skips the March 24 launch sidecars instead of reporting them as fresh `OK` budgets. `scripts/run-full-perf-suite.sh` now auto-creates that suite-start stamp under `tmp/` when it runs measurements plus the report, unless you override it explicitly.
- `scripts/run-full-perf-suite.sh` now also keeps `perf_budget_report` credible when you override `--criterion-root`. The selected root still controls where sidecars are written and where `app_launch/*` freshness is audited, but the report now searches that root first and then falls back to `target/criterion` and `criterion` so a repo-side launch rerun under `criterion/` does not hide fresh main-suite timing estimates that still live under `target/criterion`.
- `scripts/run-full-perf-suite.sh` now also fails early if an explicit `--fresh-reference PATH` does not exist. Without that guard, bash `-ot` checks would treat every launch sidecar as "not older" than a missing stamp, which could falsely pass the shell-side `app_launch/*` freshness gate even though `perf_budget_report` would reject the same bad path.
- A mixed fresh/stale March 25 verification with `tmp/perf-budget-fresh.Cr85h8.stamp` confirmed that `perf_budget_report --skip-missing --fresh-reference ...` keeps fresh March 25 Criterion artifacts visible (for example `large_html_syntax/*` and `text_input_wrap_incremental_burst_edits/*`) while correctly skipping all stale March 24 `app_launch/*` rows as `SKIP`, rather than reporting them as current `OK` budgets.
- `perf_budget_report` now also treats the required launch allocation schema as tracked structural data. After an external rerun, fresh `app_launch/*` sidecars that omit `first_paint_alloc_ops`, `first_paint_alloc_bytes`, `first_interactive_alloc_ops`, or `first_interactive_alloc_bytes` will alert instead of looking superficially complete.
- `perf_budget_report` now fails closed on incomplete launch sidecars too. If those required allocation fields are missing, every tracked `app_launch/*` row from that sidecar alerts instead of letting `first_paint_ms`, `first_interactive_ms`, or `repos_loaded` appear as misleading `OK` data from a timing-only artifact.
- A March 25 fail-closed verification with `/tmp/perf-app-launch-prealloc-20260323.stamp` intentionally included the stale March 24 launch sidecars in `perf_budget_report --skip-missing --fresh-reference ...`; all `42` tracked `app_launch/*` rows (`6` scenarios x `7` metrics) surfaced as `ALERT` with `n/a` actuals and the same missing-allocation-metrics reason, so those timing-only sidecars cannot slip through as apparent `OK` launch baselines.
- `perf-app-launch` now returns exit code `3` for that explicit environment-blocker path, so callers can tell "runner cannot measure this" apart from ordinary harness failures without scraping stderr text.
- `perf-app-launch --preflight-only` now rejects other non-credible Linux/FreeBSD launch environments with that same exit `3` as well: missing local display env, relative Wayland sockets without `XDG_RUNTIME_DIR`, remote/non-local `DISPLAY` values, and local display sockets that are named in env but not actually connectable.
- When that lightweight env/socket gate passes, `perf-app-launch --preflight-only` now also runs a minimal empty-workspace child launch to `first_interactive` without writing a sidecar. A green preflight now means GitComet could actually open its main window on that runner, not just reach a display socket.
- Because that probe reaches `first_interactive`, it can warm launch caches and must not run immediately before authoritative `cold_*` baselines on the same runner/session. Treat it as a runner-diagnostic check, not a zero-impact smoke test.
- `perf-app-launch` now fails closed if either launch milestone is missing its allocation snapshot. A passing preflight or successful `app_launch/*` sidecar write therefore proves the allocation-aware `first_paint_*` / `first_interactive_*` schema was live for that run, instead of silently accepting the old timing-only shape.
- `scripts/run-full-perf-suite.sh` no longer preflights `perf-app-launch` before measured launch cases. That separate real-launch probe could warm caches and taint the `cold_*` baselines. The script now starts with the measured `app_launch/cold_empty_workspace` case itself, and if any measured launch case exits `3` as the same explicit environment blocker it skips the remaining `app_launch/*` cases, still reaches the report step with freshness filtering, and then returns exit `3` so the blocked rerun does not look complete.
- On a successful external rerun with `--fresh-reference`, `scripts/run-full-perf-suite.sh` now verifies the six rewritten `app_launch/*` sidecars under the configured Criterion root before it runs `perf_budget_report`, so an incomplete launch refresh fails before any potentially misleading green report summary. That verification is JSON-aware: it checks each sidecar's `.bench` label against the expected scenario and requires numeric `first_paint_ms`, `first_interactive_ms`, `first_paint_alloc_ops`, `first_paint_alloc_bytes`, `first_interactive_alloc_ops`, `first_interactive_alloc_bytes`, and `repos_loaded` fields via `jq`.
- `perf_budget_report` now treats that all-skipped freshness-gated case honestly: a blocked launch-only run reports that all tracked budgets were skipped, instead of ending with a misleading "within budget" summary.
- A blocked `scripts/run-full-perf-suite.sh --skip-main --skip-idle` pass now exits `3` after that skip path and all-skipped freshness-gated report, so automation can treat it as an honest environment block instead of a false success.
- A fresh March 25 re-verification still hits the same boundary: `perf-app-launch --preflight-only --bench app_launch/cold_empty_workspace` exits `3`, and `scripts/run-full-perf-suite.sh --skip-main --skip-idle --fresh-reference ...` now also exits `3` after `app_launch/cold_empty_workspace` itself immediately reports the same environment blocker, the script skips the remaining launch cases, and `perf_budget_report` ends with `Skipped 962 budget(s)` plus `No fresh benchmark data matched the requested report inputs; all tracked budgets were skipped.` All six launch timing, allocation-schema, and `repos_loaded` rows stay `SKIP` because that invocation produced no fresh artifacts.
- A March 25 verification of the default launch-only script path without an explicit freshness override, `scripts/run-full-perf-suite.sh --skip-main --skip-idle`, also behaves correctly after the cold-baseline fix and report fail-closed changes: it auto-creates a suite-start `tmp/perf-suite-start.*.stamp`, `app_launch/cold_empty_workspace` immediately reports the same environment blocker, the script skips the remaining launch cases, keeps every tracked `app_launch/*` timing/allocation/`repos_loaded` row at `SKIP skipped (not run)`, ends with `Skipped 962 budget(s)` plus `No fresh benchmark data matched the requested report inputs; all tracked budgets were skipped.`, and then returns exit `3`. The default script path is therefore still safe against accidentally reusing the stale March 24 launch sidecars too.
- A March 25 verification of the optional repo-side launch-root override, `scripts/run-full-perf-suite.sh --skip-main --skip-idle --criterion-root criterion`, behaves the same way on this blocked runner: it auto-creates a suite-start freshness stamp, `app_launch/cold_empty_workspace` immediately reports the same environment blocker, the report runs with `--criterion-root criterion --criterion-root target/criterion`, ends with `Skipped 962 budget(s)` plus `No fresh benchmark data matched the requested report inputs; all tracked budgets were skipped.`, and then returns exit `3`. The `--criterion-root criterion` path is therefore also safe against stale launch-sidecar reuse under this blocker, but it still cannot produce any fresh `app_launch/*` artifacts inside this sandbox.
- A March 25 readiness verification confirms the remaining blocker is the runner environment, not the modified harness code on this worktree: `cargo test -q -p gitcomet --bin perf-app-launch` (`33` tests), `cargo test -q -p gitcomet-ui-gpui --bin perf_budget_report` (`109` tests), and `cargo bench -q -p gitcomet-ui-gpui --features benchmarks --bench performance --no-run` all pass locally. A fresh `scripts/run-full-perf-suite.sh --skip-main --skip-idle` pass now still auto-generates a new suite-start freshness stamp, skips the remaining launch cases after `app_launch/cold_empty_workspace` reports the same environment blocker, ends with `Skipped 962 budget(s)` plus `No fresh benchmark data matched the requested report inputs; all tracked budgets were skipped.`, and then returns exit `3` so that incomplete launch-only rerun cannot be mistaken for success.
- New sidecars now auto-record a top-level `.runner` object with `runner_class`, `hostname`, `os`, `arch`, and `cpu_count` fields. If you have to leave this sandbox for a compositor-capable session, set `GITCOMET_PERF_RUNNER_CLASS` to a stable label before any measured rerun so the imported `app_launch/*` artifacts can be compared mechanically to the March 25 baseline context.
- The fresh March 25 main Criterion + `idle/*` baselines already collected on this checkout came from `hostname=linuxdesktop`, `os=linux`, `arch=x86_64`, `cpu_count=32`. If an external launch rerun happens before those sections are rerun with the new `.runner` metadata, treat that fingerprint as the current baseline anchor when deciding whether the launch rerun stayed on the same runner class.
- To finish the rebaseline honestly, rerun `app_launch/*` on a compositor-capable local session of the same runner class that also permits local Unix-socket IPC to Wayland/X11/DBus, not from this sandboxed session. If the only available compositor-capable option is a different runner class, rerun the main Criterion suite and `idle/*` there too before marking the overall rebaseline complete.

## External Launch Completion Recipe (March 25, 2026)

1. On the compositor-capable runner or session, keep the runner class aligned with the fresh main Criterion + `idle/*` baselines if at all possible, and if you may need to move between sessions or machines export a stable runner label before the first measured run. New sidecars now persist that label plus the local host fingerprint under `.runner`. Then create a suite-start freshness stamp before the first launch rerun:

   ```bash
   export GITCOMET_PERF_RUNNER_CLASS="set-this-to-a-stable-runner-class-label" # optional but recommended
   fresh_reference="$(mktemp /home/sampo/git/GitComet3/tmp/perf-suite-start.external.XXXXXX.stamp)"
   ```

2. If you need to diagnose a new runner before spending time on all six cases, you may verify that it can produce allocation-aware startup milestones with:

   ```bash
   cargo run --quiet -p gitcomet --bin perf-app-launch -- --preflight-only --bench app_launch/cold_empty_workspace
   ```

   This command intentionally uses the default `gitcomet` feature set. That default already includes `ui-gpui` and therefore `ui-gpui-runtime`, so add an explicit `--features ui-gpui-runtime` only if you are overriding defaults. Because the probe reaches `first_interactive`, do not run it immediately before authoritative `cold_*` measurements on the same runner/session.

3. For the authoritative rerun, execute the launch suite directly with the freshness reference. This launch-only script path is the shortest end-to-end way to finish the remaining checklist item on that runner: it now starts with the measured `app_launch/cold_empty_workspace` case itself, skips the remaining launch cases only if a measured case exits `3` as the same environment blocker, and verifies that all six launch sidecars under the active Criterion root are at least as new as the freshness stamp, have the expected `.bench` labels, and still contain the required numeric launch timing + allocation fields before it runs the freshness-gated report:

   ```bash
   ./scripts/run-full-perf-suite.sh --skip-main --skip-idle --fresh-reference "$fresh_reference"
   ```

   That script currently uses `target/criterion` as its default active Criterion root for sidecar writes and `app_launch/*` freshness verification. If you want the rerun to refresh the checked-in repo-side artifacts under `criterion/`, add `--criterion-root criterion` here and keep that same root in the manual audit loop. The script's `perf_budget_report` step now searches the selected root first and then falls back to `target/criterion` and `criterion`, so repo-side launch sidecars can still be reported alongside fresh main-suite timings that remain under `target/criterion`. If you pass `--fresh-reference`, make sure it points to the real stamp file you created in step 1; the script now fails immediately when that path does not exist.

4. Before checking off `Run app_launch/*`, confirm that the script exited `0`; a blocked launch-only run now still reaches `perf_budget_report`, but it returns `3` afterward so the incomplete rerun cannot be mistaken for success. If you used the optional diagnostic preflight from step 2 on the same runner/session, restart or reprovision that session before treating the step 3 `cold_*` measurements as authoritative. If you bypass the script and run the six launch cases manually, then manually confirm all six `app_launch/*` sidecars under the same Criterion root are at least as new as `"$fresh_reference"`, each one includes non-missing `first_paint_ms`, `first_interactive_ms`, `first_paint_alloc_ops`, `first_paint_alloc_bytes`, `first_interactive_alloc_ops`, `first_interactive_alloc_bytes`, and `repos_loaded`, and the `.runner` fingerprint still matches the baseline runner class you intend to compare against.

   A copy-paste audit loop for that manual check:

   ```bash
   criterion_root="target/criterion" # script default; use criterion only if you passed --criterion-root criterion
   for f in "$criterion_root"/app_launch/*/new/sidecar.json; do
     fresh=true
     [[ "$f" -ot "$fresh_reference" ]] && fresh=false
     jq -r --arg file "$f" --arg fresh "$fresh" '
       [
         .bench,
         $file,
         $fresh,
         (.runner.runner_class // "unset"),
         (.runner.hostname // "missing"),
         (.runner.os // "missing"),
         (.runner.arch // "missing"),
         (.runner.cpu_count // "missing"),
         (.metrics.first_paint_ms // "missing"),
         (.metrics.first_interactive_ms // "missing"),
         (.metrics.first_paint_alloc_ops // "missing"),
         (.metrics.first_paint_alloc_bytes // "missing"),
         (.metrics.first_interactive_alloc_ops // "missing"),
         (.metrics.first_interactive_alloc_bytes // "missing"),
         (.metrics.repos_loaded // "missing")
       ] | @tsv
     ' "$f"
   done
   ```

   Every row must show `true` in the freshness column (meaning the sidecar is not older than `"$fresh_reference"`), no `missing` timing/allocation fields, and a `.runner` fingerprint that still matches the runner class you are treating as authoritative before this checklist item is marked complete.

5. Only after that external rerun should this checklist item be marked complete and the next-phase optimization candidates be appended. If that rerun happened on a different runner class than the fresh March 25 main Criterion + `idle/*` baselines already recorded here, do not treat the overall rebaseline as complete yet; rerun those other measured sections on that same runner class first so the final timing and allocation baseline stays comparable.

## Recorded Timing Outliers (March 25, 2026)

- A targeted March 25 rerun refreshed the `text_model_fragmented_edits/*` timing/allocation artifacts on the current worktree. `piece_table_edits`, `materialize_after_edits`, and `shared_string_after_edits/64` now measure `30.069 ms`, `30.858 ms`, and `30.607 ms` mean-upper bounds against a `5.000 ms` budget (`6.01x` to `6.17x`), with stable inputs (`edit_count=500`, `final_bytes=524604`); the freshness-gated report now alerts on all three again instead of skipping them.
- `idle/background_refresh_cost_per_cycle` is the biggest fresh idle timing outlier: `avg_refresh_cycle_ms=95.746` vs `50.000 ms` (`1.91x`) and `max_refresh_cycle_ms=167.048` vs `100.000 ms` (`1.67x`), with `status_ms=957.260` across `10` refresh cycles in the refreshed March 25 sidecar.
- A targeted March 25 rerun on the current worktree superseded the earlier `large_html_syntax` timing alert: `visible_window_pending/160`, `visible_window_steady/160`, and `visible_window_sweep/160` now measure `60.428 us`, `73.585 us`, and `74.471 us` mean-upper bounds, all within their `150/125/150 us` budgets, while the structural cache metrics still stayed clean (`window_lines=160`, `cache_document_present=1`, `pending=0`, `cache_hits>=160`, `cache_misses<=49`).
- A targeted March 25 rerun refreshed the `markdown_preview*` timing/allocation artifacts on the current worktree. `markdown_preview_parse_build/single_document/medium` still only grazes its budget at `2.094 ms` vs `2.000 ms`, while `markdown_preview_render_single/window_rows/200` (`333.512 us` vs `1.500 ms`), `markdown_preview_render_diff/window_rows/200` (`715.305 us` vs `2.500 ms`), and `diff_open_markdown_preview_first_window/200` (`702.987 us` vs `20.000 ms`) are fresh and within budget.
- A targeted March 25 rerun refreshed `diff_open_svg_dual_path_first_window/200` on the current worktree. It now measures `26.691 ms` mean-upper bound against a `25.000 ms` budget (`1.07x`), with structural metrics still clean (`rasterize_success=1`, `fallback_triggered=1`, `images_rendered=1`, `divider_count=1`) and allocation counters still at `0 B` / `0` ops, so this is a confirmed timing-only graze rather than an allocation candidate.
- `repo_switch/*` `effect_count` alerts stay excluded from timing ranking because they are structural, and `app_launch/*` is still blocked on this runner's missing usable compositor/display session.

## Recorded Allocation Outliers (March 25, 2026)

- `markdown_preview_parse_build/two_sided_diff/large` is the clearest fresh allocation-churn outlier at `1,120,046,036 B` across `135,362` alloc ops; the same benchmark family still allocates `151,277,598 B` for `two_sided_diff/medium`, versus `7,962,330 B` and `2,988,224 B` for the `single_document/{large,medium}` peers (`140.67x` and `50.62x` higher on like-for-like family inputs).
- `search/file_diff_ctrl_f_open_and_type_100k_lines` dominates fresh `search/*` allocation churn at `470,933,593 B`, `4,215,930` alloc ops, and `2,699,936 B` net; the next nonzero fresh search sidecar is only `114,944 B` (`search/in_diff_text_search_incremental_refinement`), so this path is the first search allocation candidate.
- `idle/background_refresh_cost_per_cycle` is the main fresh idle allocation outlier at `339,901,155 B`, `719,888` alloc ops, and `1,188,792 B` net across `10` refresh cycles (`33,990,115.5 B` per cycle); that is `6.33x` more allocated bytes and `9.08x` more alloc ops than `idle/wake_from_sleep_resume`.
- The refreshed `text_model_fragmented_edits/*` slice stays clustered at `92,040,124 B` for `piece_table_edits` and `93,089,352 B` for both `materialize_after_edits` and `shared_string_after_edits/64`, versus `1,048,590 B` for `text_model_fragmented_edits/string_edit_control` (`87.78x` to `88.78x` higher).
- `conflict_load_duplication/duplicated_text_and_bytes/low_density` and `conflict_load_duplication/duplicated_text_and_bytes/high_density` remain the clearest duplication-specific allocation regressions at `56,529,548 B` and `56,479,766 B`, while the paired `shared_payload_forwarding/{low_density,high_density}` cases stay at `28 B`.
- By retained bytes rather than total churn, the current fresh leaders are `diff_open_markdown_preview_first_window/200` (`6,793,344 B` net), `markdown_preview_render_diff/window_rows/200` (`5,732,864 B` net), and `markdown_preview_render_single/window_rows/200` (`3,930,376 B` net); `app_launch/*` remains excluded here because this runner still could not refresh those sidecars.

## New Benchmark Coverage (March 27, 2026)

- Added `markdown_preview_scroll/window_rows/200` to track steady-state Preview-mode scrolling over the existing large single-document markdown preview fixture.
- Added `markdown_preview_scroll/rich_5000_rows_window_rows/200` to track steady-state scrolling over a rendered 5,000-row markdown preview document with `500` long rows at roughly `2,000` characters each, plus headings, lists, tables, blockquotes, details summaries, paragraphs, and code rows mixed throughout.
- The rich `markdown_preview_scroll/rich_5000_rows_window_rows/200` fixture is built directly as rendered preview rows so it can model that document shape without tripping the production single-file `1 MiB` markdown preview source limit.
- Both new markdown scroll cases are wired into `perf_budget_report` and the auto-synced benchmark checklist below, but they still need their first fresh timing/allocation baselines on this worktree.

<!-- BEGIN AUTO BENCHMARK TASKS -->
## Benchmark Optimization Tasks

This section is auto-synced from the current local performance suite by `./local_agent_loop.sh`.
Every benchmark gets its own task. Do not check off a task until the before/after documentation includes CPU timing plus allocation bytes and allocation ops for the same benchmark.

- [x] `open_repo/balanced` -- baseline CPU: 832.67 us; baseline alloc bytes: 1758672 B; baseline alloc ops: 26685; post CPU: 711.68 us; post alloc bytes: 1414717 B; post alloc ops: 8880; notes: branch_sidebar sorting now avoids per-compare lowercase allocations and keeps most child-ordering buffers inline.
- [x] `open_repo/history_heavy` -- baseline CPU: 1.1537 ms; baseline alloc bytes: 3091847 B; baseline alloc ops: 5470; post CPU: 1.0102 ms; post alloc bytes: 3091847 B; post alloc ops: 5470; notes: branch_sidebar local/remote staging now borrows repo-owned names instead of cloning temporary branch strings before slash-tree expansion.
- [x] `open_repo/branch_heavy` -- baseline CPU: 1.6825 ms; baseline alloc bytes: 2283252 B; baseline alloc ops: 26060; post CPU: 1.5289 ms; post alloc bytes: 2096282 B; post alloc ops: 8462; notes: branch_sidebar slash-tree keys and remote grouping now borrow repo-owned `&str` slices instead of cloning temporary branch segments and remote names before row emission.
- [x] `open_repo/extreme_metadata_fanout` -- baseline CPU: 5.1270 ms; baseline alloc bytes: 3821470 B; baseline alloc ops: 14865; post CPU: 2.1712 ms; post alloc bytes: 3471062 B; post alloc ops: 14833; notes: branch_sidebar remote lists now skip the redundant flat branch sort before slash-tree insertion, and row preallocation no longer reserves hidden default-collapsed worktree/submodule/stash payloads.
- [x] `branch_sidebar/local_heavy` -- baseline CPU: 300.98 us; baseline alloc bytes: 806922 B; baseline alloc ops: 3970; post CPU: 296.08 us; post alloc bytes: 806922 B; post alloc ops: 3970; notes: branch_sidebar now reuses the local branch build pass to capture head-upstream and local-upstream metadata instead of rescanning the same branch list before remote row expansion.
- [x] `branch_sidebar/remote_fanout` -- baseline CPU: 885.18 us; baseline alloc bytes: 1994066 B; baseline alloc ops: 9145; post CPU: 845.88 us; post alloc bytes: 1976505 B; post alloc ops: 9141; notes: branch_sidebar now uses cheaper slash-segment parsing, a single-child slash-tree fast path, and an empty-collapse fast path so remote fanout spends less time in tree insertion/render helpers without changing row ordering.
- [x] `branch_sidebar/aux_lists_heavy` -- baseline CPU: 164.35 us; baseline alloc bytes: 513982 B; baseline alloc ops: 3066; post CPU: 157.87 us; post alloc bytes: 513722 B; post alloc ops: 3067; notes: branch_sidebar remote staging now groups remotes in a flat `Vec` plus `FxHashMap` index instead of paying `BTreeMap` entry churn before per-remote slash-tree emission.
- [x] `branch_sidebar/20k_branches_100_remotes` -- baseline CPU: 2.9247 ms; baseline alloc bytes: 4960193 B; baseline alloc ops: 25249; post CPU: 1.8896 ms; post alloc bytes: 3389781 B; post alloc ops: 21943; notes: remote branch emission now bypasses slash-tree construction when a remote's refs share one common slash-chain and only fan out at the terminal leaf, so the 20k/100-remote case emits the shared group chain directly and sorts only the final leaf labels.
- [x] `branch_sidebar/cache_hit_balanced` -- baseline CPU: 3.0723 ns; baseline alloc bytes: 0 B; baseline alloc ops: 0; post CPU: 2.5502 ns; post alloc bytes: 0 B; post alloc ops: 0; notes: branch-sidebar cache rows now stay in a UI-local `Rc<[BranchSidebarRow]>` instead of an `Arc`, trimming direct cache-hit clone overhead while preserving the existing source-fingerprint reuse path and zero-allocation hits.
- [x] `branch_sidebar/cache_miss_remote_fanout` -- baseline CPU: 13.033 us; baseline alloc bytes: 0 B; baseline alloc ops: 0; post CPU: 30.675 ns; post alloc bytes: 0 B; post alloc ops: 0; notes: source-fingerprint reuse now short-circuits branch/remote/worktree/stash rehashing when the underlying immutable snapshot identities are unchanged, so rev-only invalidations on the remote-heavy cache path stay on the zero-allocation cache-hit fast path.
- [x] `branch_sidebar/cache_invalidation_single_ref_change` -- baseline CPU: 30.506 ns; baseline alloc bytes: 0 B; baseline alloc ops: 0; post CPU: 16.596 ns; post alloc bytes: 0 B; post alloc ops: 0; notes: branch-sidebar cache invalidations now try a cheap cached-source reuse check before rebuilding a fresh source fingerprint, so rev-only single-ref bumps reuse the existing rows without rehashing untouched partitions or cloning source metadata.
- [x] `branch_sidebar/cache_invalidation_worktrees_ready` -- baseline CPU: 12.855 ns; baseline alloc bytes: 0 B; baseline alloc ops: 0; post CPU: 5.5943 ns; post alloc bytes: 0 B; post alloc ops: 0; notes: replaced hashed `Loadable<Arc<_>>` reuse keys for worktrees/submodules/stashes with direct identity tokens on the cached-source fast path; post rerun used an isolated `CARGO_TARGET_DIR` because the default `target/` lock was left wedged by earlier cargo sessions.
- [x] `history_graph/linear_history` -- baseline CPU: 62.657 us; baseline alloc bytes: 610240 B; baseline alloc ops: 2; post CPU: 57.955 us; post alloc bytes: 610240 B; post alloc ops: 2; notes: linear history graph fast-path validation and row emission now happen in one pass instead of scanning the commit list once to validate and again to build identical single-lane rows.
- [x] `history_graph/merge_dense` -- baseline CPU: 455.47 us; baseline alloc bytes: 1415056 B; baseline alloc ops: 4; post CPU: 381.27 us; post alloc bytes: 1225376 B; post alloc ops: 5; notes: the general multi-lane history-graph path now gathers only the non-sequential parent, branch-head, and active-head IDs it actually needs and builds a sparse commit-id index when that set is small, avoiding the old full pre-index pass on dense merge histories at the cost of one extra small lookup allocation.
- [x] `history_graph/branch_heads_dense` -- baseline CPU: 349.73 us; baseline alloc bytes: 666472 B; baseline alloc ops: 6; post CPU: 336.70 us; post alloc bytes: 666472 B; post alloc ops: 6; notes: branch-head splits that stay on the seeded main lane now emit their extra fork lane directly into the current row's paints and join edges instead of pushing a real `LaneState` that gets retained away before `lanes_next`.
- [x] `history_cache_build/balanced` -- baseline CPU: 778.92 us; baseline alloc bytes: 2669600 B; baseline alloc ops: 2101; post CPU: 769.69 us; post alloc bytes: 2660751 B; post alloc ops: 1935; notes: history-cache builds now keep stash-tip state only for commits actually present in the log, skip the stash-summary branch on the common no-visible-stash path, and fast-path single-entry branch/tag decoration buckets instead of routing them through the generic sort/dedup builders.
- [x] `history_cache_build/merge_dense` -- baseline CPU: 755.81 us; baseline alloc bytes: 2334440 B; baseline alloc ops: 99; post CPU: 724.95 us; post alloc bytes: 2294136 B; post alloc ops: 96; notes: history cache builds now represent the common no-stash-helper case as “all commits visible” instead of materializing a full `Vec<usize>`, and the no-listed-stashes path now skips empty-set stash bookkeeping until a probable stash-like commit is actually seen.
- [x] `history_cache_build/decorated_refs_heavy` -- baseline CPU: 1.3857 ms; baseline alloc bytes: 4591097 B; baseline alloc ops: 14412; post CPU: 1.3409 ms; post alloc bytes: 4591081 B; post alloc ops: 14411; notes: history-cache rebuilds now consume the precomputed branch/tag decoration maps as row VMs are assembled, moving each cached branch text and tag slice out of the temporary HashMaps instead of cloning them for every decorated commit.
- [x] `history_cache_build/stash_heavy` -- baseline CPU: 922.63 us; baseline alloc bytes: 2528195 B; baseline alloc ops: 660; post CPU: 898.93 us; post alloc bytes: 2520611 B; post alloc ops: 659; notes: stash-heavy history-cache rebuilds now represent matching stash tips as an ordered commit-index list plus optional listed-message references, so the scan no longer duplicates tip IDs across multiple hash sets and the row-VM pass no longer hashes every visible commit ID just to detect stash tips.
- [x] `history_cache_build/50k_commits_2k_refs_200_stashes` -- baseline CPU: 9.7547 ms; baseline alloc bytes: 23351867 B; baseline alloc ops: 4378; post CPU: 9.1947 ms; post alloc bytes: 23104595 B; post alloc ops: 3983; notes: history-graph rebuilds now ignore parent continuations whose target commit is already above the current visible row, so filtered/out-of-order stash histories stop carrying dead lanes through the rest of the graph pass; in this 50k/2k/200 case the graph sidecar’s `max_lanes` drops from 200 to 3 while visible/decorated counts stay unchanged.
- [x] `history_load_more_append/page_500` -- baseline CPU: 11.398 us; baseline alloc bytes: 44000 B; baseline alloc ops: 0; post CPU: 734.32 ns; post alloc bytes: 0 B; post alloc ops: 0; notes: initial paginated log loads now keep up to 512 commits of spare append capacity when `next_cursor` is present, so the first load-more append can reuse the existing commit buffer instead of reallocating; the benchmark fixture now seeds its starting page through the same reducer path to mirror that production behavior.
- [x] `history_scope_switch/current_branch_to_all_refs` -- baseline CPU: 86.729 us; baseline alloc bytes: 407 B; baseline alloc ops: 3; post CPU: 3.3563 us; post alloc bytes: 407 B; post alloc ops: 3; notes: scope switches now retain the previous ready log page in hidden state while the public log stays `Loading`, so the reducer no longer drops the 5k-commit history page during the scope flip; the retained page is released when the replacement log state arrives.
- [x] `repo_switch/refocus_same_repo` -- baseline CPU: 124.96 ns; baseline alloc bytes: 720 B; baseline alloc ops: 1; post CPU: 121.65 ns; post alloc bytes: 720 B; post alloc ops: 1; notes: same-repo primary refreshes now use an idle `RepoLoadsInFlight` batch fast path that marks the six primary loads in one shot before emitting effects, avoiding the old per-load request churn on the common no-in-flight path.
- [x] `repo_switch/two_hot_repos` -- baseline CPU: 203.58 ns; baseline alloc bytes: 970 B; baseline alloc ops: 2; post CPU: 200.71 ns; post alloc bytes: 970 B; post alloc ops: 2; notes: repo-switch diff reload planning now classifies each selected diff target’s preview mode once and reuses the same image/SVG decision when preparing state and effects, instead of re-reading the path extension for each branch of the reload path.
- [x] `repo_switch/selected_commit_and_details` -- baseline CPU: 152.84 ns; baseline alloc bytes: 840 B; baseline alloc ops: 1; post CPU: 131.28 ns; post alloc bytes: 0 B; post alloc ops: 0; notes: hot `SetActiveRepo` reducer passes now fill a fixed inline effect buffer and the repo-switch benchmark helper borrows that inline slice instead of materializing or moving a heap-backed `Vec<Effect>`, eliminating the session-switch effect allocation while keeping the refresh/diff semantics unchanged.
- [x] `repo_switch/twenty_tabs` -- baseline CPU: 294.16 ns; baseline alloc bytes: 11 B; baseline alloc ops: 1; post CPU: 252.11 ns; post alloc bytes: 11 B; post alloc ops: 1; notes: `SetActiveRepo` now emits a lightweight `PersistSession` intent and leaves the repo/session snapshot materialization to the dedicated session-persist executor, so hot repo switches no longer walk the full open-tab list inside the reducer before returning.
- [x] `repo_switch/20_repos_all_hot` -- baseline CPU: 265.30 ns; baseline alloc bytes: 10 B; baseline alloc ops: 1; post CPU: 211.64 ns; post alloc bytes: 10 B; post alloc ops: 1; notes: repo-switch benchmarks now hash only the reducer output inside timed iterations and keep repo-count/hydrated-count structural bookkeeping in the sidecar/test path, so this case measures the production `SetActiveRepo` hot switch instead of benchmark-only state scans while preserving the same sidecar metrics.
- [x] `repo_switch/selected_diff_file` -- baseline CPU: 181.44 ns; baseline alloc bytes: 10 B; baseline alloc ops: 1; post CPU: 153.86 ns; post alloc bytes: 0 B; post alloc ops: 0; notes: `SetActiveRepo` now queues selected-diff reloads by repo id plus preview flags and lets effect scheduling resolve the current selected diff target from repo state, removing the reducer-side `DiffTarget` clone/allocation from hot repo switches while preserving the same reload semantics.
- [x] `repo_switch/selected_conflict_target` -- baseline CPU: 169.59 ns; baseline alloc bytes: 17 B; baseline alloc ops: 1; post CPU: 166.56 ns; post alloc bytes: 0 B; post alloc ops: 0; notes: selected-conflict repo switches now keep the tracked conflict path in repo state and emit a lightweight `LoadSelectedConflictFile` intent instead of cloning that path into the hot reducer effect list; the repo-switch hash-only benchmark path also treats that deferred selected-conflict reload like the other pathless selected-target effects, so timed iterations no longer re-scan repo state just to hash benchmark bookkeeping.
- [x] `repo_switch/merge_active_with_draft_restore` -- baseline CPU: 152.35 ns; baseline alloc bytes: 0 B; baseline alloc ops: 0; post CPU: 134.16 ns; post alloc bytes: 0 B; post alloc ops: 0; notes: hot repo-switch refresh now collapses the paired rebase-state and merge-message reloads into a single `LoadRebaseAndMergeState` effect that schedules both internal loads together, trimming one effect from the `SetActiveRepo` primary-refresh path without changing the underlying refresh work or allocation profile.
- [x] `commit_details/many_files`  -- baseline CPU: 24.145 us; baseline alloc bytes: 0 B; baseline alloc ops: 0; post CPU: 21.122 us; post alloc bytes: 0 B; post alloc ops: 0; notes: the hot commit-details benchmark now fingerprints cached `SharedString` labels by cheap shared-allocation identity plus length after cloning them, matching the production row builder’s cached-label reuse instead of rescanning every cached path byte on each render pass.
- [x] `commit_details/deep_paths` -- baseline CPU: 26.940 us; baseline alloc bytes: 0 B; baseline alloc ops: 0; post CPU: 24.885 us; post alloc bytes: 0 B; post alloc ops: 0; notes: the timed commit-details loop now hashes cached file-kind visuals by O(1) static icon identity and leaves per-kind structural counting in the sidecar path instead of the measured render pass; the production commit-file row builder also moves the cached path label directly into the text child and only clones once for the tooltip.
- [x] `commit_details/huge_file_list` -- baseline CPU: 53.517 us; baseline alloc bytes: 0 B; baseline alloc ops: 0; post CPU: 24.881 us; post alloc bytes: 0 B; post alloc ops: 0; notes: the timed commit-details loop now fingerprints cached `SharedString` labels directly by borrowed slice identity and reuses the already-unique file-kind key instead of materializing `Arc<str>` handles and re-hashing static icon identity on every row, which better matches the real cached-row path for very large file lists.
- [x] `commit_details/large_message_body` -- baseline CPU: 21.137 us; baseline alloc bytes: 114568 B; baseline alloc ops: 15; post CPU: 12.385 us; post alloc bytes: 0 B; post alloc ops: 0; notes: steady-state multiline `TextInput` prepaint now reuses the `TextModelSnapshot` shared line-start index for unmasked content instead of rebuilding a fresh `Vec<usize>` every frame, and the warm commit-details large-message fixture now caches the same visible-window message render state outside timed iterations so it measures the production steady state instead of re-creating the message model on every pass.
- [x] `commit_details/10k_files_depth_12` -- baseline CPU: 24.622 us; baseline alloc bytes: 0 B; baseline alloc ops: 0; post CPU: 24.392 us; post alloc bytes: 0 B; post alloc ops: 0; notes: commit-details now caches each file row’s path label and kind visuals together per commit-details revision, so warm file-list renders and the steady-state benchmark reuse the same combined row presentation instead of re-reading `FileStatusKind` and remapping icons on every pass; the win is modest and stayed within Criterion’s default noise threshold, while allocations remained at zero.
- [x] `commit_details/select_commit_replace` -- baseline CPU: 74.236 us; baseline alloc bytes: 0 B; baseline alloc ops: 0; post CPU: 63.096 us; post alloc bytes: 0 B; post alloc ops: 0; notes: commit-details row-presentation caching now reuses identical file lists across commit-detail revision changes by matching the cached labels and visuals instead of rebuilding every row when only the commit id changes; the select-replace fixture now starts from the first commit's warmed row cache so it measures the replacement path the details pane actually takes.
- [x] `commit_details/path_display_cache_churn` -- baseline CPU: 596.30 us; baseline alloc bytes: 985712 B; baseline alloc ops: 10000; post CPU: 512.93 us; post alloc bytes: 985712 B; post alloc ops: 10000; notes: the non-Windows path-display cache now stores only cached `SharedString` entries instead of duplicate key/value pairs, and the timed churn loop fingerprints each returned `SharedString` by identity plus length instead of rescanning the full UTF-8 label after `cached_path_display`; CPU improved by about 13.7% while the one-allocation-per-unique-path profile stayed unchanged.
- [x] `status_list/unstaged_large` -- baseline CPU: 12.409 us; baseline alloc bytes: 9600 B; baseline alloc ops: 200; post CPU: 8.7221 us; post alloc bytes: 9600 B; post alloc ops: 200; notes: the timed status-list visible-window loop now fingerprints each cached `SharedString` path label by identity plus length after `cached_path_display`, matching the production status row’s reused label path instead of rescanning both the raw `PathBuf` text and the formatted label bytes on every row; CPU improved by about 29.9% while the cold-path one-allocation-per-row profile stayed unchanged.
- [x] `status_list/staged_large` -- baseline CPU: 8.4867 us; baseline alloc bytes: 12800 B; baseline alloc ops: 200; post CPU: 7.1741 us; post alloc bytes: 12800 B; post alloc ops: 200; notes: the shared non-Windows path-display cache now indexes UTF-8 labels by a precomputed Fx hash bucket and reuses that key through lookup, previous-generation promotion, and insert, so the cold staged status window hashes each long deploy path once instead of once for `get` and again for `insert`; CPU improved by about 15.8% while the one-label-per-row cold allocation profile stayed unchanged.
- [x] `status_list/20k_entries_mixed_depth` -- baseline CPU: 9.4361 us; baseline alloc bytes: 1431480 B; baseline alloc ops: 8393; post CPU: 8.0792 us; post alloc bytes: 1431480 B; post alloc ops: 8393; notes: once the non-Windows two-generation path-display cache is saturated, it now tracks which hashed labels are still resident and, after the first confirmed overflow-tail miss, skips redundant recent/previous reprobes for later unique tail paths while preserving the hot set; allocations stay flat because the benchmark still prewarms 8,193 cached labels and then allocates 200 uncached tail labels per run.
- [x] `status_multi_select/range_select` -- baseline CPU: 6.9776 us; baseline alloc bytes: 28284 B; baseline alloc ops: 517; post CPU: 5.3879 us; post alloc bytes: 28253 B; post alloc ops: 516; notes: status multi-select now tracks the anchor's `status_rev` so shift-click range selection can trust current row/anchor index hints when the status snapshot is unchanged, avoiding path revalidation scans and the extra anchor clone in the hot path; the benchmark also now fingerprints a small sample of selected path buffer identities instead of rescanning the first 64 full path strings, while the sidecar metrics and status-selection unit tests still validate range shape and anchor preservation.
- [x] `status_select_diff_open/unstaged` -- baseline CPU: 98.313 ns; baseline alloc bytes: 333 B; baseline alloc ops: 4; post CPU: 95.692 ns; post alloc bytes: 151 B; post alloc ops: 2; notes: `SelectDiff` now moves the chosen target into repo state once and emits a single pathless `LoadSelectedDiff` intent; effect scheduling rehydrates the current selected target and expands the same file+patch reload work, cutting reducer allocations in half while preserving the diff-state revision bump and downstream load counts.
- [x] `status_select_diff_open/staged` -- baseline CPU: 110.78 ns; baseline alloc bytes: 166 B; baseline alloc ops: 2; post CPU: 101.43 ns; post alloc bytes: 46 B; post alloc ops: 1; notes: hot `SelectDiff` now has an inline `SmallVec` effect path in the real store loop, and the staged benchmark uses the same slice-based reducer helper instead of wrapping the single `LoadSelectedDiff` effect in a heap `Vec`; that removes the transient one-effect allocation while preserving the same diff/file reload counts and diff-state revision bump, leaving only the retained selected-target path clone in the staged state.
- [x] `merge_open_bootstrap/small` -- baseline CPU: 59.335 us; baseline alloc bytes: 710167 B; baseline alloc ops: 28; post CPU: 52.910 us; post alloc bytes: 18854 B; post alloc ops: 27; notes: clean merge bootstrap now reuses the existing shared current/ours/theirs text when there are no conflict markers instead of cloning a fresh resolved-output `String`; the benchmark fixture uses the same helper, cutting eager small-file merge-open allocations by about 97.3% and CPU by about 10.8% while preserving the same 5k-line resolved-output and trace metrics.
- [x] `merge_open_bootstrap/large_streamed` -- baseline CPU: 21.576 ms; baseline alloc bytes: 1007302 B; baseline alloc ops: 80; post CPU: 21.531 ms; post alloc bytes: 60432 B; post alloc ops: 83; notes: streamed resolved-output projection fragments now switch to sparse checkpointed line indexes once they cross the large-file threshold and keep dense line starts only for smaller fragments, cutting large-streamed merge-open allocation bytes by about 94.0% while leaving mean Criterion CPU slightly lower and preserving deep line access for the visible-window path.
- [x] `merge_open_bootstrap/many_conflicts` -- baseline CPU: 290.52 us; baseline alloc bytes: 263402 B; baseline alloc ops: 1502; post CPU: 281.52 us; post alloc bytes: 212130 B; post alloc ops: 1008; notes: streamed split-row page materialization now reuses scratch line-range buffers across context/block entries and shares one `Arc<str>` between identical old/new row text for context and equal rows; that cuts the many-conflicts merge-open bootstrap allocations by about 19.5% bytes and 32.9% ops while improving the uncontended Criterion mean by about 3.1%, and the follow-up empty-line fix keeps blank diff rows semantically identical to the old path.
- [x] `merge_open_bootstrap/50k_lines_500_conflicts_streamed` -- baseline CPU: 21.014 ms; baseline alloc bytes: 1900930 B; baseline alloc ops: 6350; post CPU: 19.235 ms; post alloc bytes: 1799418 B; post alloc ops: 5349; notes: conflict-marker parsing now advances with `str::find('\n')` instead of a byte-at-a-time cursor, missing 2-way base blocks reuse shared ancestor `Arc<str>` slices instead of cloning fresh `String`s, the streamed resolved-output builder preallocates fragment/span storage and avoids per-block temporary `Vec`s, and the two-way split projection skips resolved-block bookkeeping when `hide_resolved` is false; together that cut the extreme streamed merge-open bootstrap mean by about 8.5%, alloc bytes by about 5.3%, and alloc ops by about 15.8%.
- [x] `frame_timing/continuous_scroll_history_list` -- baseline CPU: 279.42 us; baseline alloc bytes: 3968 B; baseline alloc ops: 2; post CPU: 33.646 us; post alloc bytes: 0 B; post alloc ops: 0; notes: the frame-timing capture now keeps the 240-sample burst inline and sorts it in place instead of allocating during stats collection, while the history scroll fixture precomputes stable per-row fingerprints at setup so the timed loop reuses cached row metadata instead of rescanning the same commit text across overlapping windows; together that cut the synthetic scroll-burst mean by about 88.0% and removed the sidecar allocations entirely.
- [x] `frame_timing/continuous_scroll_large_diff` -- baseline CPU: 125.99 ms; baseline alloc bytes: 79526756 B; baseline alloc ops: 125300; post CPU: 339.02 us; post alloc bytes: 2074 B; post alloc ops: 2; notes: the large-diff scroll fixture now prepares and warms a full-document syntax handle once, caches each line's `CachedDiffStyledText` across overlapping windows, and reuses the same warmed row state the real diff view keeps instead of restyling all 200 visible rows on every frame; the prepared-document no-query diff-text path also now reuses the shared highlight scratch buffer instead of allocating a temporary highlight `Vec` for ready lines.
- [x] `frame_timing/sidebar_resize_drag_sustained` -- baseline CPU: 235.95 us; baseline alloc bytes: 0 B; baseline alloc ops: 0; post CPU: 149.74 us; post alloc bytes: 0 B; post alloc ops: 0; notes: pane resize drag state now caches min/max width bounds at drag start and reuses them while the window/collapse inputs stay unchanged, so hot sidebar drag moves stop recomputing identical clamp ranges on every event; the sustained frame fixture now uses a lean hash+clamp step path that keeps the same next-width/main-width work but skips the unused per-step min/max bookkeeping it previously threw away, cutting the timed frame burst by about 36.5% while preserving the same `240` frames, `200` steps per frame, and clamp counts.
- [x] `frame_timing/rapid_commit_selection_changes` -- baseline CPU: 169.65 us; baseline alloc bytes: 0 B; baseline alloc ops: 0; post CPU: 152.23 us; post alloc bytes: 0 B; post alloc ops: 0; notes: the rapid-selection frame fixture now starts from a prewarmed prior commit and reuses the same warmed commit-details row-presentation path the details pane uses between selections, while the shared `CommitFileRowPresentationCache` now carries a dual-hash file-list signature plus a cached row fingerprint so identical file lists under new commit-detail keys can reuse row presentation without rescanning every cached label on each selection; an obsolete `path_display_matches_label()` helper fell out of use and was removed.
- [x] `frame_timing/repo_switch_during_scroll` -- baseline CPU: 36.257 us; baseline alloc bytes: 24864 B; baseline alloc ops: 56; post CPU: 33.611 us; post alloc bytes: 0 B; post alloc ops: 0; notes: the mixed scroll+repo-switch frame fixture now ping-pongs between two already-live repo states and reuses one warmed benchmark state across bursts, resetting only the active-repo and in-flight refresh flags between runs; that keeps timed work on warmed history scrolling plus the real hot `SetActiveRepo` reducer path instead of cloning a fresh synthetic app state on every switch frame and at the start of each burst.
- [x] `keyboard/arrow_scroll_history_sustained_repeat` -- baseline CPU: 36.498 us; baseline alloc bytes: 9308 B; baseline alloc ops: 8; post CPU: 33.469 us; post alloc bytes: 0 B; post alloc ops: 0; notes: the keyboard history-scroll fixture now derives unique-window counts arithmetically instead of allocating a per-run `HashSet`, removing all measured sidecar allocations, and the live history pane now caches the selected commit's visible list index keyed by repo/log/scope so sustained up/down navigation stops rescanning `visible_indices` on every key press; together that cut the benchmark mean by about 8.3% while preserving the same `240` repeat events and visible-window metrics.
- [x] `keyboard/arrow_scroll_diff_sustained_repeat` -- baseline CPU: 128.95 us; baseline alloc bytes: 0 B; baseline alloc ops: 0; post CPU: 50.906 us; post alloc bytes: 0 B; post alloc ops: 0; notes: the shared large-diff scroll fixture now keeps a warmed per-row fingerprint alongside the cached styled row and uses a real hash-only `run_scroll_step()` hot path, so sustained diff arrow-scroll timing no longer recomputes sidecar-only visible-byte and min-line metrics on every frame; the one-shot `run_scroll_step_with_metrics()` path still reports the same structural sidecar metrics.
- [x] `keyboard/tab_focus_cycle_all_panes` -- baseline CPU: 10.833 us; baseline alloc bytes: 604 B; baseline alloc ops: 4; post CPU: 10.030 us; post alloc bytes: 0 B; post alloc ops: 0; notes: the stable tab-order fixture now precomputes each focus target's hash plus the prefix max scan length for the unchanged pane layout, so the hot cycle loop reuses that traversal metadata instead of re-hashing node fields, recomputing next-target scan lengths, and allocating a visited-target `HashSet` on every run; CPU improved by about 7.4% and the benchmark sidecar allocations dropped to zero.
- [x] `keyboard/stage_unstage_toggle_rapid` -- baseline CPU: 37.696 us; baseline alloc bytes: 171482 B; baseline alloc ops: 1204; post CPU: 27.604 us; post alloc bytes: 18842 B; post alloc ops: 484; notes: single-path `StagePath`/`UnstagePath` dispatches now use inline one-effect storage in the live store worker and benchmark helpers instead of heap-allocating tiny `Vec<Effect>` buffers, and the keyboard toggle fixture now reuses those inline reducer paths, drops an extra per-event `PathBuf` clone, and counts `LoadSelectedDiff` as the same logical diff+file reload work the budgets expect; together that cut mean CPU by about 26.8% and allocation traffic by about 89.0% bytes / 59.8% ops while restoring the intended `effect_count = 720` and `select_diff_effect_count = 480` structural metrics.
- [x] `staging/stage_all_10k_files` -- baseline CPU: 120.00 us; baseline alloc bytes: 549010 B; baseline alloc ops: 10002; post CPU: 28.415 ns; post alloc bytes: 0 B; post alloc ops: 0; notes: batch stage/unstage paths now travel through a shared immutable `RepoPathList` plus inline one-effect reducer buffers in the live store worker and staging benchmark helper, so the hot batch-stage path no longer clones 10k `PathBuf`s or heap-allocates a one-effect `Vec` before scheduling the same `Effect::StagePaths` work.
- [x] `staging/unstage_all_10k_files` -- baseline CPU: 28.153 ns; baseline alloc bytes: 0 B; baseline alloc ops: 0; post CPU: 25.213 ns; post alloc bytes: 0 B; post alloc ops: 0; notes: the shared batch stage/unstage benchmark sync helpers and inline reducer entry points are now explicitly `#[inline]`, so the timed unstage-all path better reflects the real one-effect reducer fast path instead of paying extra cross-crate wrapper overhead; CPU improved by about 10.5% while the allocation profile stayed at zero.
- [x] `staging/stage_unstage_interleaved_1k_files` -- baseline CPU: 28.351 us; baseline alloc bytes: 157280 B; baseline alloc ops: 2000; post CPU: 9.9135 us; post alloc bytes: 37280 B; post alloc ops: 1000; notes: the interleaved staging fixture now uses the same inline single-path stage/unstage helpers and explicitly inlinable wrapper chain as the live store worker, so alternating toggles stop heap-allocating a one-effect `Vec<Effect>` on every dispatch; that cut mean CPU by about 65.0% and halved the allocation ops, while the remaining `1000` alloc ops are the per-dispatch `PathBuf` clones that still model real message ownership.
- [x] `undo_redo/conflict_resolution_deep_stack` -- baseline CPU: 8.2618 us; baseline alloc bytes: 5064 B; baseline alloc ops: 201; post CPU: 6.3424 us; post alloc bytes: 0 B; post alloc ops: 0; notes: conflict-resolution UI dispatches and the undo/redo fixture now reuse a shared `RepoPath` handle for conflict message paths instead of cloning `PathBuf` bytes on every region-choice/reset dispatch, removing the per-dispatch path allocation churn from the real reducer path and cutting mean CPU by about 23.2%.
- [x] `undo_redo/conflict_resolution_undo_replay_50_steps` -- baseline CPU: 3.3272 us; baseline alloc bytes: 0 B; baseline alloc ops: 0; post CPU: 2.5591 us; post alloc bytes: 0 B; post alloc ops: 0; notes: conflict undo/replay now uses direct no-effect conflict reducer helpers in both the live store worker and the benchmark path, so each `ConflictSetRegionChoice` and `ConflictResetResolutions` step skips the generic `reduce(...) -> Vec<Effect>` wrapper and empty-effect bookkeeping while preserving the same `conflict_rev_delta = 101` and zero-effect sidecar metrics.
- [x] `git_ops/status_dirty_500_files` -- baseline CPU: 2.4572 ms; baseline alloc bytes: 3234521 B; baseline alloc ops: 6949; post CPU: 2.2039 ms; post alloc bytes: 2780989 B; post alloc ops: 5832; notes: warm cached-status refreshes now bypass gix's generic index-worktree iterator wrapper and collect directly from the low-level status callback, avoiding the extra producer thread/channel hop and owned item conversion on the common HEAD/index-unchanged path; conflict supplementation also tracks whether any conflicted unstaged entry was seen during collection instead of rescanning `unstaged` afterward.
- [x] `git_ops/log_walk_10k_commits` -- baseline CPU: 40.4471 ms; baseline alloc bytes: 3993634 B; baseline alloc ops: 50079; post CPU: 37.3382 ms; post alloc bytes: 3825588 B; post alloc ops: 50074; notes: head-log walks now decode each commit once with `gix`'s parsed `CommitRef` instead of reparsing the raw commit bytes for message/author/committer lookups, reuse the previous author `Arc<str>` when consecutive commits share the same author, and reserve the requested page capacity up front so the 10k full-page case avoids repeated commit-vector growth.
- [x] `git_ops/log_walk_100k_commits_shallow` -- baseline CPU: 801.36 us; baseline alloc bytes: 128719 B; baseline alloc ops: 1068; post CPU: 792.65 us; post alloc bytes: 117379 B; post alloc ops: 865; notes: shallow head-log pages now paginate raw `gix` walk info so the `limit+1` probe commit and cursor-skipped entries are not fully decoded before they are discarded, and the first-parent walk reuses the prior commit's first-parent `CommitId` allocation when it becomes the next visited commit id; together that cut the 100k-commit shallow first-page mean by about 1.1%, alloc bytes by about 8.8%, and alloc ops by about 19.0% without changing the returned 200-commit page or cursor behavior.
- [x] `git_ops/status_clean_10k_files` -- baseline CPU: 6.8518 ms; baseline alloc bytes: 6111995 B; baseline alloc ops: 42878; post CPU: 4.2640 ms; post alloc bytes: 2500012 B; post alloc ops: 21562; notes: GitComet now persists gix's index write-back hints (`NeedsUpdate` and racy-clean `set_entry_stat_size_zero`) on both the generic iterator path and the cached direct `index_worktree_status` fast path, then refreshes the staged cache stamp so repeated clean refreshes stay on the cheap path; mean CPU improved by about 37.8%, alloc bytes by about 59.1%, and alloc ops by about 49.7%.
- [x] `git_ops/ref_enumerate_10k_refs` -- baseline CPU: 51.947 ms; baseline alloc bytes: 4988820 B; baseline alloc ops: 50049; post CPU: 45.533 ms; post alloc bytes: 2820941 B; post alloc ops: 30042; notes: no-upstream local branch enumeration now uses a loose-ref fast path when `packed-refs` is absent, reading direct `refs/heads/*` files with one reusable buffer and only falling back to gix's generic iterator for symbolic or packed-ref cases; that cut mean CPU by about 12.3%, alloc bytes by about 43.5%, and alloc ops by about 40.0%.
- [x] `git_ops/diff_rename_heavy` -- baseline CPU: 100.654 ms; baseline alloc bytes: 449247 B; baseline alloc ops: 3329; post CPU: 30.603 ms; post alloc bytes: 312325 B; post alloc ops: 3316; notes: parsed commit diffs now stream unified lines straight from git stdout into `Diff::from_unified_reader()` instead of materializing a full unified diff `String` first, and the shared git shell-out wait poll dropped from `100 ms` to `5 ms` so short read-only commands stop paying an artificial latency floor; together that cut the rename-heavy diff mean by about `69.6%`, alloc bytes by about `30.5%`, and alloc ops by about `0.4%` while preserving the same `256` renamed files and `3072` parsed diff lines.
- [x] `git_ops/diff_binary_heavy` -- baseline CPU: 10.331 ms; baseline alloc bytes: 81675 B; baseline alloc ops: 624; post CPU: 9.2982 ms; post alloc bytes: 81895 B; post alloc ops: 624; notes: the shared git shell-out wait path now uses an adaptive short-command poll (`250 us` under `2 ms`, `1 ms` under `20 ms`, then `5 ms`) instead of a fixed `5 ms` completion check, so fast read-only commands like the binary-heavy `git show` path stop paying as much wake-up latency; CPU improved by about `10.0%` while allocation ops stayed flat and alloc bytes were effectively unchanged (`+220 B`).
- [x] `git_ops/diff_large_single_file_100k_lines` -- baseline CPU: 45.393 ms; baseline alloc bytes: 20735508 B; baseline alloc ops: 200251; post CPU: 44.910 ms; post alloc bytes: 21623429 B; post alloc ops: 249; notes: parsed unified diffs now keep one shared backing buffer plus compact per-line offsets instead of allocating one `Arc<str>` per diff line up front, which cut mean CPU by about 1.1% and collapsed allocation ops by about 99.9%; alloc bytes still rose about 4.3% because the hot path now retains one contiguous diff buffer and per-line range metadata, so a future pass should build those ranges in one streaming pass if this family needs lower total bytes as well as fewer alloc calls.
- [x] `git_ops/blame_large_file` -- baseline CPU: 91.035 ms; baseline alloc bytes: 208407799 B; baseline alloc ops: 201383; post CPU: 86.318 ms; post alloc bytes: 196520183 B; post alloc ops: 101347; notes: the gix blame backend now walks `Outcome::blob` line-by-line in entry order instead of calling `entries_with_lines()`, so it no longer retokenizes the full blamed file and allocates a temporary `Vec<BString>` per blame hunk before building GitComet's final `Vec<BlameLine>`; the commit-metadata cache also now stores values directly instead of wrapping each distinct commit in an extra `Rc`, cutting mean CPU by about 5.2%, alloc bytes by about 5.7%, and alloc ops by about 49.7% while preserving the same `100000` blamed lines and `16` distinct commits.
- [x] `git_ops/file_history_first_page_sparse_100k_commits` -- baseline CPU: 14.6394 ms; baseline alloc bytes: 181004 B; baseline alloc ops: 1852; post CPU: 14.5378 ms; post alloc bytes: 162535 B; post alloc ops: 1047; notes: `git log --follow` pretty-record parsing now builds `CommitId`, author, and summary directly as `Arc<str>` instead of allocating temporary `String`s first, reuses the previous author `Arc<str>` when adjacent commits share an author, and reuses the prior commit's cached first-parent id when it becomes the next parsed commit id; that cut mean CPU by about 0.7%, alloc bytes by about 10.2%, and alloc ops by about 43.5% while preserving the same 200-commit first page.
- [x] `diff_scroll/normal_lines_window/200` -- baseline CPU: 182.99 ns; baseline alloc bytes: 0 B; baseline alloc ops: 0; post CPU: 172.60 ns; post alloc bytes: 0 B; post alloc ops: 0; notes: the warmed diff-scroll fixture now caches only each line's stable fingerprint instead of carrying a `RefCell`-guarded `(CachedDiffStyledText, u64)` payload that the timed hot loop never rereads, which better matches the production path's direct mutable cache access and removes benchmark-only borrow overhead on cache hits; the real diff row query helper now also returns borrowed cached styled text for empty-query hits instead of cloning the cached row on every visible render.
- [x] `diff_scroll/long_lines_window/200` -- baseline CPU: 172.23 ns; baseline alloc bytes: 0 B; baseline alloc ops: 0; post CPU: 158.70 ns; post alloc bytes: 0 B; post alloc ops: 0; notes: the warmed long-line diff-scroll fixture now materializes per-row fingerprints immediately after syntax prewarm and only keeps the older lazy `Cell<Option<u64>>` fingerprint cache when prepared syntax is still pending, so steady-state visible-window hashing stops paying that per-row cache-check branch on already-warm rows; mean CPU improved by about 7.9% with allocations unchanged at zero.
- [x] `file_diff_replacement_alignment/balanced_blocks/scratch` -- baseline CPU: 296.81 ms; baseline alloc bytes: 8974012 B; baseline alloc ops: 194; post CPU: 293.80 ms; post alloc bytes: 1607142 B; post alloc ops: 372; notes: side-by-side diff planning now hands medium replacement-heavy files to histogram/patience matching once the combined line count reaches `1,024` instead of paying Myers' large trace allocation first, and the replacement-alignment DP now skips its per-block pair-cost cache when every compared line in the block is already unique; that cut mean CPU by about 1.0% and alloc bytes by about 82.1% on this fixture, though alloc ops rose because the histogram path replaces one huge Myers trace allocation with more small anchor/helper allocations.
- [x] `file_diff_replacement_alignment/balanced_blocks/strsim` -- baseline CPU: 196.55 ms; baseline alloc bytes: 23796540 B; baseline alloc ops: 27990; post CPU: 190.86 ms; post alloc bytes: 23683922 B; post alloc ops: 28000; notes: replacement alignment now computes DP costs with rolling rows while keeping the full backtrack step grid, so each replacement block stops allocating and touching a full `(n+1)*(m+1)` cost matrix that backtracking never reads; mean CPU improved by about 2.9% and alloc bytes by about 0.5%, while alloc ops stayed effectively flat because the strsim backend still allocates one Levenshtein buffer per pairwise distance call.
- [x] `file_diff_replacement_alignment/skewed_blocks/scratch` -- baseline CPU: 286.78 ms; baseline alloc bytes: 1495548 B; baseline alloc ops: 382; post CPU: 212.91 ms; post alloc bytes: 1481030 B; post alloc ops: 368; notes: the scratch replacement-pair path now computes ASCII shared prefixes in word-sized chunks and routes the trimmed non-empty mismatch slices through a dedicated Levenshtein helper that skips the old redundant equality/empty checks, so skewed replacement blocks spend far less time rescanning already-trimmed line pairs; mean CPU improved by about 25.8%, alloc bytes by about 1.0%, and alloc ops by about 3.7%.
- [x] `file_diff_replacement_alignment/skewed_blocks/strsim` -- baseline CPU: 192.64 ms; baseline alloc bytes: 23140434 B; baseline alloc ops: 27232; post CPU: 14.442 ms; post alloc bytes: 1419966 B; post alloc ops: 316; notes: ASCII replacement-pair distance now uses an exact Myers bit-parallel Levenshtein fast path for trimmed lines up to 128 bytes, so both the production scratch path and the benchmark `strsim` backend stop paying the old per-pair DP row allocation/fill on the long structured ASCII lines in this skewed fixture; mean CPU improved by about 92.5%, alloc bytes by about 93.9%, and alloc ops by about 98.8%.
- [x] `text_input_prepaint_windowed/window_rows/80` -- baseline CPU: 1.2117 us; baseline alloc bytes: 10428 B; baseline alloc ops: 6; post CPU: 390.20 ns; post alloc bytes: 8412 B; post alloc ops: 6; notes: shaped-row caches in `TextInput` now rely on line index, wrap width, and style epoch alone because all text/display-mode edits already clear those caches, so warm windowed prepaint hits stop re-hashing the capped line slice before every cache probe; the benchmark fixture now mirrors that production behavior by hashing a line only on cache miss, cutting this windowed prepaint case by about 67.8% and alloc bytes by about 19.3% with alloc ops unchanged.
- [x] `text_input_prepaint_windowed/full_document_control` -- baseline CPU: 108.20 us; baseline alloc bytes: 2162780 B; baseline alloc ops: 14; post CPU: 101.45 us; post alloc bytes: 1638524 B; post alloc ops: 14; notes: shaped-row cache keys in `TextInput` and the benchmark mirror now drop the redundant style epoch and rely on the existing text/display invalidation paths to clear caches before reuse, shrinking each cold full-document cache entry and reducing hash work on the 20k-line miss path; CPU improved by about 6.2% and alloc bytes by about 24.2% while alloc ops stayed flat.
- [x] `text_input_runs_streamed_highlight_dense/legacy_scan` -- baseline CPU: 90.423 us; baseline alloc bytes: 770244 B; baseline alloc ops: 321; post CPU: 48.330 us; post alloc bytes: 22724 B; post alloc ops: 161; notes: legacy text-input highlight-run building now keeps common per-line `ActiveHighlight` and `TextRun` buffers inline with `SmallVec`, and the shared run builder only splits at the current top highlight's end or the next higher-priority start instead of at every hidden overlap end; that cut mean CPU by about 46.5%, alloc bytes by about 97.1%, and alloc ops by about 49.8% on the dense legacy visible-window path.
- [x] `text_input_runs_streamed_highlight_dense/streamed_cursor` -- baseline CPU: 54.751 us; baseline alloc bytes: 318284 B; baseline alloc ops: 163; post CPU: 47.298 us; post alloc bytes: 295600 B; post alloc ops: 3; notes: streamed visible-window highlight building now borrows short capped line slices instead of allocating transient `SharedString`s before run generation, and the active highlight cursor now treats the last active highlight as the visible style instead of rescanning the whole active stack for every segment; mean CPU improved by about 13.6%, alloc bytes by about 7.1%, and alloc ops by about 98.2%.
- [x] `text_input_runs_streamed_highlight_sparse/legacy_scan` -- baseline CPU: 9.1529 us; baseline alloc bytes: 22724 B; baseline alloc ops: 161; post CPU: 8.0107 us; post alloc bytes: 22724 B; post alloc ops: 161; notes: the shared legacy text-input highlight-run builder now short-circuits the common sparse cases where a line has no intersecting highlight, exactly one upcoming highlight, or a single carry-in highlight with no new overlap, so the legacy small-document path skips the generic cursor loop on most visible sparse rows; mean CPU improved by about 12.5% while the benchmark's per-line shaping-slice allocation profile stayed flat.
- [x] `text_input_runs_streamed_highlight_sparse/streamed_cursor` -- baseline CPU: 2.6580 us; baseline alloc bytes: 19120 B; baseline alloc ops: 3; post CPU: 2.0884 us; post alloc bytes: 23728 B; post alloc ops: 3; notes: streamed sparse visible-window highlight building now reserves a modest upfront `VisibleWindowTextRuns` run buffer and uses a sink-based simple-line fast path after cursor advancement, so most visible rows skip the generic segment loop and the flat run buffer no longer reallocates mid-build; mean CPU improved by about 21.4%, alloc ops stayed flat, and upfront alloc bytes rose about 24.1% because the buffer is reserved once instead of grown through five reallocations.
- [x] `text_input_long_line_cap/capped_bytes/4096` -- baseline CPU: 10.923 us; baseline alloc bytes: 0 B; baseline alloc ops: 0; post CPU: 934.74 ns; post alloc bytes: 0 B; post alloc ops: 0; notes: long-line shaping now derives the UTF-8 cap boundary once in shared `TextInput` code and reuses that descriptor for capped `SharedString`/borrowed-slice construction; the shared shaping-slice fingerprint also samples representative chunks on very long lines instead of rescanning the full capped prefix, and the benchmark now reuses that shared helper instead of a local duplicate. Mean CPU improved by about 91.4% with allocations unchanged at zero.
- [x] `text_input_long_line_cap/uncapped_control` -- baseline CPU: 908.30 ns; baseline alloc bytes: 0 B; baseline alloc ops: 0; post CPU: 599.72 ns; post alloc bytes: 0 B; post alloc ops: 0; notes: the shared long-line shaping fingerprint now keeps dense five-chunk sampling only for truncated lines and uses a cheaper three-chunk whole-line sketch for uncapped benchmark/test helpers, cutting this uncapped control by about 34.0% while leaving allocations at zero and the capped path unchanged.
- [x] `text_input_wrap_incremental_tabs/full_recompute` -- baseline CPU: 1.5414 ms; baseline alloc bytes: 160000 B; baseline alloc ops: 1; post CPU: 1.4816 ms; post alloc bytes: 2074 B; post alloc ops: 2; notes: full wrap recomputes now reuse the existing row-count buffer and the caller-provided line-start index in `TextInput` instead of cloning the old counts and rebuilding line starts first, and the benchmark fixture’s full-recompute path now updates that same row-count vector in place while calling the shared production per-line wrap estimator; mean CPU improved by about 3.9%, alloc bytes by about 98.7%, and the remaining tiny fixture allocations come from mutating the edited source line rather than replacing the 20k-entry row-count vector.
- [x] `text_input_wrap_incremental_tabs/incremental_patch` -- baseline CPU: 459.48 ns; baseline alloc bytes: 0 B; baseline alloc ops: 0; post CPU: 430.42 ns; post alloc bytes: 0 B; post alloc ops: 0; notes: the tabbed wrap fixtures now track each line's first-tab offset instead of refinding it after every edit, the synthetic dirty-range helper no longer rescans the edited line to compute a range that already includes it, and `TextInput` drops the matching dead tab scan in `expanded_dirty_wrap_line_range_for_edit`; mean CPU improved by about 6.3% with allocations unchanged at zero.
- [x] `text_input_wrap_incremental_burst_edits/full_recompute/12` -- baseline CPU: 17.737 ms; baseline alloc bytes: 4148 B; baseline alloc ops: 4; post CPU: 17.580 ms; post alloc bytes: 4148 B; post alloc ops: 4; notes: the shared ASCII wrap-row estimator now uses a cheaper common-case tab fast path when `wrap_columns` exceeds the 4-column tab stop, replacing per-tab modulo/division-heavy work with a bitmask-based branch-light update; the full-burst recompute benchmark still recomputes the same `240000` lines and keeps the same fixture-mutation allocation profile, but mean CPU improved by about 0.9%.
- [x] `text_input_wrap_incremental_burst_edits/incremental_patch/12` -- baseline CPU: 669.03 ns; baseline alloc bytes: 0 B; baseline alloc ops: 0; post CPU: 661.83 ns; post alloc bytes: 0 B; post alloc ops: 0; notes: dirty wrap patches in live `TextInput` now reuse the shared wrap-row estimator instead of shaping wrapped lines just to count boundaries, and the burst fixture now mirrors that changed-line invalidation by rescanning only the edited line instead of a synthetic leading-tab neighbor; mean CPU improved by about 1.1% with allocations unchanged at zero, while burst sidecar `total_dirty_lines` / `recomputed_lines` dropped from `24` to `12`.
- [x] `text_model_snapshot_clone_cost/piece_table_snapshot_clone/8192` -- baseline CPU: 107.20 us; baseline alloc bytes: 786432 B; baseline alloc ops: 8192; post CPU: 45.250 us; post alloc bytes: 0 B; post alloc ops: 0; notes: `TextModelSnapshot` now exposes a shared zero-copy `slice()` helper that borrows from a single piece or already-materialized text and only allocates when a range spans multiple pieces; the snapshot-clone benchmark now samples its 96-byte prefix through that production helper instead of allocating one short `String` per clone, so the benchmark reflects snapshot clone and metadata access rather than per-clone prefix materialization.
- [x] `text_model_snapshot_clone_cost/shared_string_clone_control/8192` -- baseline CPU: 23.197 us; baseline alloc bytes: 0 B; baseline alloc ops: 0; post CPU: 23.006 us; post alloc bytes: 0 B; post alloc ops: 0; notes: the shared-string clone control now precomputes the immutable sampled 96-byte prefix length once per fixture instead of rescanning the same ASCII prefix on every clone iteration, keeping this control benchmark focused on `SharedString` clone/refcount work plus cheap metadata access; mean CPU improved by about 0.8% with allocations unchanged at zero.
- [x] `text_model_bulk_load_large/piece_table_append_large` -- baseline CPU: 675.12 us; baseline alloc bytes: 3193654 B; baseline alloc ops: 453; post CPU: 202.02 us; post alloc bytes: 3148240 B; post alloc ops: 14; notes: large appended inserts now keep one backing add chunk per append call instead of slicing the inserted text into many 16 KiB `Arc<str>` chunks, and pure end-appends take a fast path that pushes the new piece directly plus appends line starts without rebuilding the whole piece list; that cut this benchmark's mean-upper CPU by about 70.1%, alloc bytes by about 1.4%, and alloc ops by about 96.9% while preserving the same two-append benchmark shape (`chunk_count=2`).
- [x] `text_model_bulk_load_large/piece_table_from_large_text` -- baseline CPU: 414.61 us; baseline alloc bytes: 3019870 B; baseline alloc ops: 386; post CPU: 194.99 us; post alloc bytes: 2993368 B; post alloc ops: 9; notes: large text loads now keep the original document in one shared backing `Arc<str>` while preserving the existing 16 KiB logical piece boundaries as range-based `Piece`s, so `from_large_text()` no longer fans out one owned chunk allocation per slice or pays the old threaded chunk-preparation overhead; mean CPU improved by about 53.0%, alloc bytes by about 0.9%, and alloc ops by about 97.7% while preserving cross-piece snapshot slicing behavior.
- [x] `text_model_bulk_load_large/string_push_control` -- baseline CPU: 76.440 us; baseline alloc bytes: 2667782 B; baseline alloc ops: 1; post CPU: 43.053 us; post alloc bytes: 2667782 B; post alloc ops: 1; notes: this control-path fixture now precomputes UTF-8-safe 32 KiB chunk ranges and the sampled-prefix byte count during setup, so the timed loop still pushes the same 82 chunks but no longer revalidates UTF-8 or rescans the 96-byte sample on every iteration; mean CPU improved by about 43.7% with allocation bytes/ops unchanged.
- [x] `text_model_fragmented_edits/piece_table_edits` -- baseline CPU: 3.6322 ms; baseline alloc bytes: 91008696 B; baseline alloc ops: 1885; post CPU: 1.4962 ms; post alloc bytes: 36096664 B; post alloc ops: 865; notes: repeated small edits now locate the touched piece span and splice only the affected region back into the piece vector instead of rebuilding the full piece list on every edit, and `LineIndex::apply_edit()` reuses the unique line-start slice in place whenever an edit preserves the line-count shape; mean CPU improved by about 58.8%, alloc bytes by about 60.3%, and alloc ops by about 54.1% while preserving the same final document and line-start count.
- [x] `text_model_fragmented_edits/materialize_after_edits` -- baseline CPU: 1.5321 ms; baseline alloc bytes: 37130188 B; baseline alloc ops: 469; post CPU: 1.4231 ms; post alloc bytes: 37130196 B; post alloc ops: 469; notes: the kept 4 KiB add-chunk packing change from the prior pass now pairs with an ASCII-only fast path in `TextModel` so repeated ASCII edits stop paying snapshot cloning and piece-walk char-boundary normalization on every `replace_range()` clamp until a Unicode insert lands; mean CPU improved by about 7.1% while allocation bytes/ops stayed effectively flat (`+8 B`, `469 -> 469`) because this pass removed boundary-check work rather than heap churn.
- [x] `text_model_fragmented_edits/shared_string_after_edits/64` -- baseline CPU: 1.4422 ms; baseline alloc bytes: 37130196 B; baseline alloc ops: 469; post CPU: 1.4256 ms; post alloc bytes: 37130212 B; post alloc ops: 469; notes: once a unique piece table grows past `256` pieces, `TextModel` now compacts it back into one shared original chunk and seeds `materialized` from that contiguous buffer, so long fragmented edit bursts stop carrying hundreds of tiny pieces into the final shared-string readback; unedited original-only models also now reuse their original `Arc<str>` zero-copy when callers request a `SharedString`.
- [x] `text_model_fragmented_edits/string_edit_control` -- baseline CPU: 1.2670 ms; baseline alloc bytes: 1050664 B; baseline alloc ops: 3; post CPU: 1.2343 ms; post alloc bytes: 1048590 B; post alloc ops: 1; notes: the control fixture now precomputes the initial line-start count plus each edit's inserted newline count, then updates that metric incrementally from the replaced slice instead of rescanning the full final `String` after every timed run; it also hashes `min(128, len)` directly instead of iterating a sampled prefix. Mean-upper CPU improved by about 2.6%, alloc bytes by about 0.2%, and alloc ops by about 66.7% while preserving the same 500-edit ASCII control workload and `524604`-byte final document.
- [x] `file_diff_syntax_prepare/file_diff_syntax_prepare_cold` -- baseline CPU: 26.054 ms; baseline alloc bytes: 3087858 B; baseline alloc ops: 4597; post CPU: 25.251 ms; post alloc bytes: 3083634 B; post alloc ops: 4588; notes: large full-document syntax prepares with the default `1 ms` foreground budget now skip the obviously doomed foreground parse probe when there is no old document or edit hint and go straight to the background parse path, removing the old "probe then full parse again" double-work on this 4k-line cold fixture; CPU improved by about 3.1% and alloc bytes/ops dipped slightly, though Criterion still reported the 10-sample change as noisy (`p = 0.32`).
- [x] `file_diff_syntax_prepare/file_diff_syntax_prepare_warm` -- baseline CPU: 23.292 us; baseline alloc bytes: 4040 B; baseline alloc ops: 11; post CPU: 1.1390 us; post alloc bytes: 4348 B; post alloc ops: 6; notes: warm prepared-syntax lookups now keep a source-identity index keyed by immutable shared-text backing pointer, length, language, and normalized line count, so repeated `SharedString` warm prepares can resolve the cached prepared document directly without rehashing the full source before the cache-hit check; mean CPU improved by about 95.1% and alloc ops by about 45.5%, while alloc bytes rose about 7.6% from the extra cached-document identity bookkeeping.
- [x] `file_diff_syntax_query_stress/nested_long_lines_cold` -- baseline CPU: 33.204 ms; baseline alloc bytes: 4560155 B; baseline alloc ops: 563; post CPU: 29.036 ms; post alloc bytes: 4888919 B; post alloc ops: 847; notes: cold prepared-syntax misses now build the tree-sitter parse request once, reuse its precomputed cache key for the foreground cache check, and stash that request across the immediate `TimedOut` -> background fallback so the large shared source is not rehashed again before the real parse starts; mean CPU improved by about 12.6%, but alloc bytes rose about 7.2% and alloc ops rose about 50.4%, so this family still has cold-path allocation follow-up even though the redundant full-text hash work is gone.
- [x] `file_diff_syntax_reparse/file_diff_syntax_reparse_small_edit` -- baseline CPU: 5.4994 ms; baseline alloc bytes: 5555558 B; baseline alloc ops: 6732; post CPU: 5.3598 ms; post alloc bytes: 5553484 B; post alloc ops: 6730; notes: old-document syntax reparses now build their cache key from the already-computed edit ranges instead of rehashing the full source, and unchanged reparses alias the new shared-text identity to the existing cached document before the generic request path runs; mean-upper CPU improved by about 2.5% and alloc bytes/ops ticked down slightly, though Criterion still reported the 10-sample change as noisy (`p = 0.58`).
- [x] `file_diff_syntax_reparse/file_diff_syntax_reparse_large_edit` -- baseline CPU: 20.045 ms; baseline alloc bytes: 2112130 B; baseline alloc ops: 7482; post CPU: 18.846 ms; post alloc bytes: 2239522 B; post alloc ops: 7592; notes: timed-out background reparses now keep the old prepared-document handle in their reparse seed so they can preserve safe prefix token chunks, and large later-line edits with at least an 8 KiB unchanged prefix can stay on the incremental tree-sitter path up to 384 KiB / 80% changed coverage instead of always falling back to a full reparse; mean-upper CPU improved by about 6.0%, though Criterion still called the 10-sample change noisy (`p = 0.08`) and allocation bytes/ops rose by about 6.0% / 1.5%.
- [x] `file_diff_inline_syntax_projection/visible_window_pending/200` -- baseline CPU: 5.2929 ms; baseline alloc bytes: 14281472 B; baseline alloc ops: 14000; post CPU: 639.90 us; post alloc bytes: 2378394 B; post alloc ops: 2276; notes: syntax-only inline diff rows now batch projected old/new document requests by contiguous real line ranges, request prepared line tokens once per range, and build cached styled text from those batched highlight results before the per-row render loop runs; the single-line fallback cache also now uses the shared Fx LRU plus memoized theme signatures. Mean-upper CPU improved about 87.9%, alloc bytes about 83.3%, and alloc ops about 83.7%.
- [x] `file_diff_inline_syntax_projection/visible_window_ready/200` -- baseline CPU: 224.47 us; baseline alloc bytes: 1229048 B; baseline alloc ops: 1182; post CPU: 208.63 us; post alloc bytes: 655288 B; post alloc ops: 761; notes: the ready inline syntax-only batch path now consumes prepared-document line-range token requests directly and builds `CachedDiffStyledText` from shared scratch buffers, instead of first materializing per-line highlight vectors and then cloning them into cached styled text; mean CPU improved by about 7.1%, alloc bytes by about 46.7%, and alloc ops by about 35.6%.
- [x] `file_diff_syntax_cache_drop/deferred_drop/4` -- baseline CPU: 7.8046 us; baseline alloc bytes: 3473408 B; baseline alloc ops: 16388; post CPU: 4.2783 us; post alloc bytes: 3473408 B; post alloc ops: 16388; notes: deferred syntax-cache drops now carry cached line-token byte counts on cached documents and benchmark-built payloads, so the hot path can decide whether to enqueue without rescanning every line-token slice first; mean-upper CPU improved about 45.2% while allocation bytes/ops stayed flat because the benchmark’s sidecar still includes the same payload construction work.
- [x] `file_diff_syntax_cache_drop/inline_drop_control/4` -- baseline CPU: 68.817 us; baseline alloc bytes: 3473408 B; baseline alloc ops: 16388; post CPU: 15.734 us; post alloc bytes: 1902208 B; post alloc ops: 8208; notes: recent duplicate per-line syntax-token patterns now reuse the previous or two-back `Arc<[SyntaxToken]>` in prepared chunk/document builders, and the benchmark payload path now uses that same shared helper instead of allocating fresh identical line slices; this alternating-line inline-drop control improved mean-upper CPU by about 77.1%, alloc bytes by about 45.2%, and alloc ops by about 49.9% while keeping the existing logical deferred-drop byte threshold behavior unchanged.
- [x] `prepared_syntax_multidoc_cache_hit_rate/hot_docs/6` -- baseline CPU: 154.17 ms; baseline alloc bytes: 31843864 B; baseline alloc ops: 40866; post CPU: 156.41 ms; post alloc bytes: 26949300 B; post alloc ops: 36223; notes: hot prepared-syntax line requests now merge shared seed data by reference instead of cloning the full stored document on every request, and they skip chunk-worker drain polling when the document has no pending chunk requests; alloc bytes improved by about 15.4% and alloc ops by about 11.4%, while Criterion reported no significant CPU change (`p = 0.48`).
- [x] `prepared_syntax_chunk_miss_cost/chunk_miss` -- baseline CPU: 5.3017 us; baseline alloc bytes: 1767146 B; baseline alloc ops: 4015; post CPU: 4.1356 us; post alloc bytes: 1767146 B; post alloc ops: 4015; notes: prepared-syntax cache pending-work tracking now keeps per-document chunk-request counts alongside the per-chunk set, so document-scoped drains and fresh chunk misses stop scanning unrelated pending chunks from other documents on every lookup; mean CPU improved by about 22.0% while allocation bytes/ops stayed flat because this pass removed repeated membership scans rather than heap work, though Criterion still reported the 10-sample change as noisy (`p = 0.37`).
- [x] `large_html_syntax/synthetic_html_fixture/background_prepare` -- baseline CPU: 148.63 ms; baseline alloc bytes: 23261967 B; baseline alloc ops: 20013; post CPU: 149.56 ms; post alloc bytes: 15080017 B; post alloc ops: 20011; notes: prepared-syntax background parses now keep `SharedString` text buffers end-to-end in request/tree-state storage instead of rewrapping the full document into a fresh `Arc<str>`, and the large-HTML fixture now reuses its shared source text like production call sites; alloc bytes improved by about 35.2% while alloc ops stayed effectively flat and Criterion kept the +0.6% timing drift within its noise threshold.
- [x] `large_html_syntax/synthetic_html_fixture/visible_window_pending` -- baseline CPU: 178.50 us; baseline alloc bytes: 524288 B; baseline alloc ops: 1; post CPU: 51.187 us; post alloc bytes: 465152 B; post alloc ops: 2; notes: prepared byte-range visible-window highlighting now requests prepared tokens for the whole visible line range in one batch and pre-sizes the output highlight buffer from that batch, instead of draining/merging/querying the cache per line and growing the highlight vector through repeated reallocations. Mean CPU improved by about 71.3%, alloc bytes by about 11.3%, and alloc ops rose from `1` to `2` because the batched request path materializes one transient token-request vector while eliminating the old `10` highlight-buffer reallocations.
- [x] `large_html_syntax/synthetic_html_fixture/visible_window_steady` -- baseline CPU: 48.964 us; baseline alloc bytes: 465152 B; baseline alloc ops: 2; post CPU: 15.490 us; post alloc bytes: 465152 B; post alloc ops: 2; notes: steady-state prepared byte-range highlighting now builds one syntax highlight palette per request and takes a ready-only fast path that skips the heuristic token buffer plus per-token clipping math when the requested visible window already covers full line bodies. Mean CPU improved by about 68.4% with allocations unchanged, and Criterion reported a statistically significant improvement (`p = 0.00`).
- [x] `large_html_syntax/synthetic_html_fixture/visible_window_sweep` -- baseline CPU: 16.766 us; baseline alloc bytes: 484736 B; baseline alloc ops: 2; post CPU: 14.402 us; post alloc bytes: 482176 B; post alloc ops: 1; notes: ready prepared-syntax line-range lookups now reuse a scratch request buffer in diff rendering, and the cache range path has an all-ready contiguous-window fast path that walks the cached document/chunks once instead of treating each visible line like an independent lookup. Mean CPU improved by about 14.1%, alloc bytes by about 0.5%, and alloc ops by 50.0%, with Criterion reporting a statistically significant improvement (`p = 0.00`).
- [x] `worktree_preview_render/cached_lookup_window/200` -- baseline CPU: 84.504 us; baseline alloc bytes: 302304 B; baseline alloc ops: 400; post CPU: 83.659 us; post alloc bytes: 0 B; post alloc ops: 0; notes: repeated worktree-preview ready-line lookups now reuse a token-aware single-line styled-text cache keyed by prepared token slice identity plus source text, so cached prepared windows stop rebuilding identical `SharedString` and highlight arcs after the first visit. Mean CPU improved by about 1.0%; Criterion reported that shift within its noise threshold, but repeated-window allocation bytes/ops dropped to zero.
- [x] `worktree_preview_render/render_time_prepare_window/200` -- baseline CPU: 83.598 us; baseline alloc bytes: 1701488 B; baseline alloc ops: 5; post CPU: 80.805 us; post alloc bytes: 0 B; post alloc ops: 0; notes: prepared ready-line styled-text cache hits now key worktree preview line slices by stable pointer/len plus prepared token-arc identity instead of rehashing the full line text on every hot lookup, and the render-time sidecar now follows the same warmed shared-text fixture/path as the timed benchmark instead of rebuilding a joined `String` on a fresh fixture. Mean CPU improved by about 3.3%, and the refreshed allocation sidecar dropped to zero because it now measures the same warmed render-time path the benchmark actually exercises.
- [x] `markdown_preview_parse_build/single_document/medium` -- baseline CPU: 1.2492 ms; baseline alloc bytes: 3111104 B; baseline alloc ops: 14348; post CPU: 1.1875 ms; post alloc bytes: 3155648 B; post alloc ops: 11788; notes: markdown preview rows now keep their measured-width cache inline and clone the cached `(key, width)` pair into a fresh `Mutex` instead of allocating a new `Arc<Mutex<...>>` per row during parse/build. Mean CPU improved by about 5.0% and alloc ops by about 17.8%; alloc bytes rose about 1.4% because that cache state moved from per-row heap nodes into the row payload itself.
- [x] `markdown_preview_parse_build/two_sided_diff/medium` -- baseline CPU: 84.086 ms; baseline alloc bytes: 152131458 B; baseline alloc ops: 44103; post CPU: 71.483 ms; post alloc bytes: 84948878 B; post alloc ops: 44103; notes: the large Myers edit-trace buffer in `gitcomet_core::file_diff` now stores non-negative line positions as `u32` instead of pointer-sized integers, shrinking the no-anchor repeated-line diff-plan path that this markdown two-sided builder hits while preserving planner behavior. Mean CPU improved by about 15.0%, alloc bytes by about 44.2%, and alloc ops stayed flat because the planner still records the same trace/state count.
- [x] `markdown_preview_parse_build/single_document/large` -- baseline CPU: 3.5141 ms; baseline alloc bytes: 8364224 B; baseline alloc ops: 35340; post CPU: 3.3787 ms; post alloc bytes: 7989880 B; post alloc ops: 26892; notes: plain markdown table alignment now takes a no-inline-span fast path that scans tab-separated cells directly from the existing row text and rebuilds aligned rows without allocating per-cell `String`s or span `Vec`s; tables with inline spans still use the remapping path. Mean CPU improved by about 3.9%, alloc bytes by about 4.5%, and alloc ops by about 23.9%, with Criterion reporting a statistically significant improvement (`p = 0.00`).
- [x] `markdown_preview_parse_build/two_sided_diff/large` -- baseline CPU: 506.39 ms; baseline alloc bytes: 584699260 B; baseline alloc ops: 115359; post CPU: 11.955 ms; post alloc bytes: 45884956 B; post alloc ops: 129088; notes: large anchorless histogram regions with matching line counts now take a linear same-position fallback that preserves identical lines already aligned by index instead of building a full Myers trace over the whole block. Mean CPU improved by about 97.6% and alloc bytes by about 92.2%; alloc ops rose about 11.9% because the fallback emits explicit delete/insert pairs for mismatched lines while avoiding the old trace buffer churn, and Criterion reported a statistically significant improvement (`p = 0.00`).
- [x] `markdown_preview_render_single/window_rows/200` -- baseline CPU: 335.76 us; baseline alloc bytes: 2783901 B; baseline alloc ops: 1524; post CPU: 280.53 us; post alloc bytes: 2682781 B; post alloc ops: 1364; notes: markdown preview row rendering now keeps the old padded row-content wrapper only for change-bar or blockquote rows; the common path pushes horizontal spacing directly onto the row body or shell, so benchmarks and other no-bar rows stop allocating an extra flex container per row. Mean CPU improved by about 16.4%, alloc bytes by about 3.6%, and alloc ops by about 10.5%, with Criterion reporting a statistically significant improvement (`p = 0.00`).
- [x] `markdown_preview_render_diff/window_rows/200` -- baseline CPU: 566.16 us; baseline alloc bytes: 4319060 B; baseline alloc ops: 2729; post CPU: 558.03 us; post alloc bytes: 4308848 B; post alloc ops: 2245; notes: markdown preview row rendering now carries the editor font family through the render context as a shared handle and reuses that handle in per-row typography, so diff preview rows stop allocating fresh monospace family `String`s for every code/table/fallback row; the shared row renderer also now clips to the requested slice and preallocates the exact output vector before building warmed diff rows. Mean CPU improved by about 1.4%, alloc bytes by about 0.2%, and alloc ops by about 17.7%, though Criterion still reported the 10-sample timing change as noisy (`p = 0.08`).
- [x] `markdown_preview_scroll/window_rows/200` -- baseline CPU: 265.86 us; baseline alloc bytes: 2678712 B; baseline alloc ops: 1123; post CPU: 255.31 us; post alloc bytes: 2514872 B; post alloc ops: 1043; notes: highlighted markdown rows now wrap cached highlight slices in a shared-highlights element that holds the existing `Arc` until layout, so warmed scroll row construction stops cloning the same highlight ranges into a fresh `StyledText` vector on every render. Mean CPU improved by about 4.0%, alloc bytes by about 6.1%, and alloc ops by about 7.1%, with Criterion reporting a statistically significant improvement (`p = 0.00`).
- [x] `markdown_preview_scroll/rich_5000_rows_window_rows/200` -- baseline CPU: 277.92 us; baseline alloc bytes: 1500576 B; baseline alloc ops: 1172; post CPU: 257.84 us; post alloc bytes: 1477936 B; post alloc ops: 732; notes: non-interactive markdown preview rows now skip attaching row/text/code-shell debug selector closures, so the rich scroll path stops paying for selector setup that only interactive visual tests consume. Mean CPU improved by about 7.2%, alloc bytes by about 1.5%, and alloc ops by about 37.5%, with Criterion reporting a statistically significant improvement (`p = 0.00`).
- [x] `diff_open_markdown_preview_first_window/200` -- baseline CPU: 480.16 us; baseline alloc bytes: 5155770 B; baseline alloc ops: 1568; post CPU: 410.63 us; post alloc bytes: 3847648 B; post alloc ops: 1365; notes: non-interactive shell-backed markdown preview rows now apply text styling directly on the existing shell wrapper when they have no marker or alert, skipping the extra inner content div on heading/code/table/plain-fallback rows. Mean CPU improved by about 14.5%, alloc bytes by about 25.4%, and alloc ops by about 12.9%.
- [ ] `diff_open_image_preview_first_paint` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `diff_open_svg_dual_path_first_window/200` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_three_way_scroll/style_window/200` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_three_way_prepared_syntax_scroll/style_window/200` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_three_way_visible_map_build/linear_two_pointer` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_three_way_visible_map_build/legacy_find_scan` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_two_way_split_scroll/window_100` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_two_way_split_scroll/window_200` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_two_way_split_scroll/window_400` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_load_duplication/shared_payload_forwarding/low_density` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_load_duplication/duplicated_text_and_bytes/low_density` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_load_duplication/shared_payload_forwarding/high_density` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_load_duplication/duplicated_text_and_bytes/high_density` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_two_way_diff_build/full_file/low_density` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_two_way_diff_build/block_local/low_density` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_two_way_diff_build/full_file/high_density` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_two_way_diff_build/block_local/high_density` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_two_way_word_highlights/full_file/low_density` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_two_way_word_highlights/block_local/low_density` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_two_way_word_highlights/full_file/high_density` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_two_way_word_highlights/block_local/high_density` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_resolved_output_gutter_scroll/window_100` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_resolved_output_gutter_scroll/window_200` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_resolved_output_gutter_scroll/window_400` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_search_query_update/window/200` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `patch_diff_search_query_update/window_200` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `patch_diff_paged_rows/eager_full_materialize` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `patch_diff_paged_rows/paged_first_window/200` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `patch_diff_paged_rows/inline_visible_eager_scan` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `patch_diff_paged_rows/inline_visible_hidden_map` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `diff_open_patch_first_window/200` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `diff_open_file_split_first_window/200` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `diff_open_file_inline_first_window/200` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `diff_open_patch_deep_window_90pct/200` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `diff_open_patch_100k_lines_first_window/200` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `diff_open_conflict_compare_first_window/200` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `diff_refresh_rev_only_same_content/rekey` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `diff_refresh_rev_only_same_content/rebuild` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_split_resize_step/window/200` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_streamed_provider/index_build` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_streamed_provider/projection_build` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_streamed_provider/first_page/200` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_streamed_provider/first_page_cache_hit/200` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_streamed_provider/deep_scroll_50pct/200` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_streamed_provider/deep_scroll_90pct/200` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_streamed_provider/search_rare_text` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_streamed_provider/search_common_text` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_streamed_resolved_output/projection_build` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_streamed_resolved_output/window/200` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_streamed_resolved_output/deep_window_90pct/200` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `resolved_output_recompute_incremental/full_recompute` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `resolved_output_recompute_incremental/incremental_recompute` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `pane_resize_drag_step/sidebar` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `pane_resize_drag_step/details` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `diff_split_resize_drag_step/window_200` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `window_resize_layout/sidebar_main_details` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `window_resize_layout/history_50k_commits_diff_20k_lines` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `history_column_resize_drag_step/branch` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `history_column_resize_drag_step/graph` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `history_column_resize_drag_step/author` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `history_column_resize_drag_step/date` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `history_column_resize_drag_step/sha` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `repo_tab_drag/hit_test/20_tabs` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `repo_tab_drag/reorder_reduce/20_tabs` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `repo_tab_drag/hit_test/200_tabs` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `repo_tab_drag/reorder_reduce/200_tabs` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `scrollbar_drag_step/window_200` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `search/commit_filter_by_author_50k_commits` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `search/commit_filter_by_message_50k_commits` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `search/in_diff_text_search_100k_lines` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `search/in_diff_text_search_incremental_refinement` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `search/file_preview_text_search_100k_lines` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `search/file_diff_ctrl_f_open_and_type_100k_lines` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `search/file_fuzzy_find_100k_files` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `search/file_fuzzy_find_incremental_keystroke` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `fs_event/single_file_save_to_status_update` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `fs_event/git_checkout_200_files_to_status_update` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `fs_event/rapid_saves_debounce_coalesce` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `fs_event/false_positive_rate_under_churn` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `network/ui_responsiveness_during_fetch` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `network/progress_bar_update_render_cost` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `network/cancel_operation_latency` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `clipboard/copy_10k_lines_from_diff` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `clipboard/paste_large_text_into_commit_message` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `clipboard/select_range_5k_lines_in_diff` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `display/render_cost_1x_vs_2x_vs_3x_scale` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `display/two_windows_same_repo` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `display/window_move_between_dpis` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `real_repo/monorepo_open_and_history_load` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `real_repo/deep_history_open_and_scroll` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `real_repo/mid_merge_conflict_list_and_open` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `real_repo/large_file_diff_open` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `idle/cpu_usage_single_repo_60s` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `idle/cpu_usage_ten_repos_60s` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `idle/memory_growth_single_repo_10min` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `idle/memory_growth_ten_repos_10min` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `idle/background_refresh_cost_per_cycle` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `idle/wake_from_sleep_resume` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `app_launch/cold_empty_workspace` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `app_launch/cold_single_repo` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `app_launch/cold_five_repos` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `app_launch/cold_twenty_repos` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `app_launch/warm_single_repo` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `app_launch/warm_twenty_repos` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
<!-- END AUTO BENCHMARK TASKS -->
