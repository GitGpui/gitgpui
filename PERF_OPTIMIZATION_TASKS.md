# Performance Optimization Task List

Fresh run reset on March 24, 2026.

This file is intentionally cleared. Recreate tasks only after the suite has been rebaselined with the new allocation metrics.

## Rebaseline Checklist

- [ ] Run the main Criterion suite
- [ ] Run `app_launch/*` on a compositor-capable runner/session that permits local Wayland/X11/DBus socket IPC
- [ ] Run `idle/*`
- [ ] Run `perf_budget_report --skip-missing`
- [ ] Record the first timing outliers
- [ ] Record the first allocation outliers

## Current Rebaseline Blocker (March 25, 2026)

- `app_launch/*` remains blocked on this runner. `perf-app-launch` now fails at harness preflight, before child launch, with an environment blocker even though `DISPLAY=:0`, `WAYLAND_DISPLAY=wayland-0`, `XDG_RUNTIME_DIR=/run/user/1000`, and `XAUTHORITY=/run/user/1000/.mutter-Xwaylandauth.HT10M3` are present.
- A direct March 25 rerun of all six `app_launch/*` scenarios now fails identically at that preflight with exit `3`, before fixture setup or child spawn, so repeating different `app_launch/*` cases on this runner will not produce fresh timing or allocation data until the environment changes.
- Direct Unix-socket probes now confirm this is not just a missing env-var issue: `nc -U /run/user/1000/wayland-0` and `nc -U /tmp/.X11-unix/X0` both fail with `Operation not permitted`, and `dbus-run-session` cannot bind its own local socket under `/tmp` or `/run/user/1000`. This sandboxed runner is denying the local socket IPC that GPUI, X11, and Wayland need.
- A further March 25 environment check confirmed the restriction is broader than those display endpoints: `xdpyinfo` cannot open `DISPLAY=:0`, Python cannot even create or bind fresh AF_UNIX / AF_INET sockets (`socket()` / `bind()` return `Operation not permitted`), abstract Unix sockets fail the same way, and `Xvfb` cannot open `inet`, `inet6`, or `local` listeners. There is no credible local socket-based workaround for `app_launch/*` inside this sandbox.
- GPUI headless mode is not a credible `app_launch/*` fallback on this platform. The Linux/FreeBSD headless client cannot open the main window, while the startup probe records `first_paint` / `first_interactive` from real window prepaint/next-frame callbacks.
- A March 25 reference-repo audit confirmed there is no hidden Linux offscreen launch path to reuse here. In `zed`, `gpui_linux::current_platform(true)` switches to `HeadlessClient`, `HeadlessClient::open_window()` still bails instead of creating a real window, and `gpui_platform::current_headless_renderer()` returns `None` on non-macOS. There is no Linux headless renderer path in the upstream GPUI stack that could credibly emit comparable `first_paint` / `first_interactive` launch milestones inside this sandbox.
- The tracked `criterion/app_launch/*/new/sidecar.json` artifacts are still the March 24, 2026 baseline (`04:58:05` through `04:58:21` local) and must not be treated as fresh March 25 rebaseline data on this runner.
- There are still no `target/criterion/app_launch/*/new/sidecar.json` artifacts on this checkout either. A fresh March 25, 2026 `cargo run --quiet -p gitcomet --bin perf-app-launch -- --preflight-only --bench app_launch/cold_empty_workspace` recheck still exits `3` with the same local Wayland/X11 `Operation not permitted` socket-probe failures, so the default local Criterion root still has no credible `app_launch/*` baseline.
- A broader March 25, 2026 audit under `/home/sampo` also found no newer `app_launch/*/new/sidecar.json` artifacts anywhere else on this machine. Do not spend more local turns searching this filesystem again unless a fresh external rerun is explicitly copied in.
- Those six tracked March 24 launch sidecars are also pre-allocation-schema artifacts: they only contain `first_paint_ms`, `first_interactive_ms`, RSS, and `repos_loaded`, with no `first_paint_*` / `first_interactive_*` allocation counters. Even if their timings were otherwise acceptable, they still cannot satisfy the current allocation-aware rebaseline.
- Do not check off `Run app_launch/*` until all six launch cases (`cold_empty_workspace`, `cold_single_repo`, `cold_five_repos`, `cold_twenty_repos`, `warm_single_repo`, `warm_twenty_repos`) have been rerun on a compositor-capable session, their `app_launch/*` sidecars under the active Criterion root (`target/criterion` by default for local reruns, or the path passed via `--criterion-root` / `GITCOMET_PERF_CRITERION_ROOT`) have fresh mtimes from that rerun, and each sidecar includes non-missing milestone allocation fields such as `first_paint_alloc_bytes`, `first_paint_alloc_ops`, `first_interactive_alloc_bytes`, and `first_interactive_alloc_ops` alongside `first_paint_ms`, `first_interactive_ms`, and `repos_loaded`.
- `perf_budget_report` now needs a freshness gate when stale `app_launch/*` sidecars remain on disk under the active Criterion root. Use `--fresh-reference PATH` (where `PATH` predates the fresh suite run) so the report skips the March 24 launch sidecars instead of reporting them as fresh `OK` budgets. `scripts/run-full-perf-suite.sh` now auto-creates that suite-start stamp under `tmp/` when it runs measurements plus the report, unless you override it explicitly.
- `scripts/run-full-perf-suite.sh` now also keeps `perf_budget_report` credible when you override `--criterion-root`. The selected root still controls where sidecars are written and where `app_launch/*` freshness is audited, but the report now searches that root first and then falls back to `target/criterion` and `criterion` so a repo-side launch rerun under `criterion/` does not hide fresh main-suite timing estimates that still live under `target/criterion`.
- `scripts/run-full-perf-suite.sh` now also fails early if an explicit `--fresh-reference PATH` does not exist. Without that guard, bash `-ot` checks would treat every launch sidecar as "not older" than a missing stamp, which could falsely pass the shell-side `app_launch/*` freshness gate even though `perf_budget_report` would reject the same bad path.
- A mixed fresh/stale March 25 verification with `tmp/perf-budget-fresh.Cr85h8.stamp` confirmed that `perf_budget_report --skip-missing --fresh-reference ...` keeps fresh March 25 Criterion artifacts visible (for example `large_html_syntax/*` and `text_input_wrap_incremental_burst_edits/*`) while correctly skipping all stale March 24 `app_launch/*` rows as `SKIP`, rather than reporting them as current `OK` budgets.
- `perf_budget_report` now also treats the required launch allocation schema as tracked structural data. After an external rerun, fresh `app_launch/*` sidecars that omit `first_paint_alloc_ops`, `first_paint_alloc_bytes`, `first_interactive_alloc_ops`, or `first_interactive_alloc_bytes` will alert instead of looking superficially complete.
- `perf_budget_report` now fails closed on incomplete launch sidecars too. If those required allocation fields are missing, every tracked `app_launch/*` row from that sidecar alerts instead of letting `first_paint_ms`, `first_interactive_ms`, or `repos_loaded` appear as misleading `OK` data from a timing-only artifact.
- A March 25 fail-closed verification with `/tmp/perf-app-launch-prealloc-20260323.stamp` intentionally included the stale March 24 launch sidecars in `perf_budget_report --skip-missing --fresh-reference ...`; all `42` tracked `app_launch/*` rows (`6` scenarios x `7` metrics) surfaced as `ALERT` with `n/a` actuals and the same missing-allocation-metrics reason, so those timing-only sidecars cannot slip through as apparent `OK` launch baselines.
- `perf-app-launch` now returns exit code `3` for that explicit environment-blocker path, so callers can tell "runner cannot measure this" apart from ordinary harness failures without scraping stderr text.
- `perf-app-launch --preflight-only` now rejects other non-credible Linux/FreeBSD launch environments with that same exit `3` as well: missing local display env, relative Wayland sockets without `XDG_RUNTIME_DIR`, remote/non-local `DISPLAY` values, and local display sockets that are named in env but not actually connectable.
- When that lightweight env/socket gate passes, `perf-app-launch --preflight-only` now also runs a minimal empty-workspace child launch to `first_interactive` without writing a sidecar. A green preflight now means GitComet could actually open its main window on that runner, not just reach a display socket.
- Because that probe reaches `first_interactive`, it can warm launch caches and must not run immediately before authoritative `cold_*` baselines on the same runner/session. Treat it as a runner-diagnostic check, not a zero-impact smoke test.
- `perf-app-launch` now fails closed if either launch milestone is missing its allocation snapshot. A passing preflight or successful `app_launch/*` sidecar write therefore proves the allocation-aware `first_paint_*` / `first_interactive_*` schema was live for that run, instead of silently accepting the old timing-only shape.
- `scripts/run-full-perf-suite.sh` no longer preflights `perf-app-launch` before measured launch cases. That separate real-launch probe could warm caches and taint the `cold_*` baselines. The script now starts with the measured `app_launch/cold_empty_workspace` case itself, and if any measured launch case exits `3` as the same explicit environment blocker it skips the remaining `app_launch/*` cases, still reaches the report step with freshness filtering, and then returns exit `3` so the blocked rerun does not look complete.
- On a successful external rerun with `--fresh-reference`, `scripts/run-full-perf-suite.sh` now verifies the six rewritten `app_launch/*` sidecars under the configured Criterion root before it runs `perf_budget_report`, so an incomplete launch refresh fails before any potentially misleading green report summary. That verification is JSON-aware: it checks each sidecar's `.bench` label against the expected scenario and requires numeric `first_paint_ms`, `first_interactive_ms`, `first_paint_alloc_ops`, `first_paint_alloc_bytes`, `first_interactive_alloc_ops`, `first_interactive_alloc_bytes`, and `repos_loaded` fields via `jq`.
- `perf_budget_report` now treats that all-skipped freshness-gated case honestly: a blocked launch-only run reports that all tracked budgets were skipped, instead of ending with a misleading "within budget" summary.
- A blocked `scripts/run-full-perf-suite.sh --skip-main --skip-idle` pass now exits `3` after that skip path and all-skipped freshness-gated report, so automation can treat it as an honest environment block instead of a false success.
- A fresh March 25 re-verification still hits the same boundary: `perf-app-launch --preflight-only --bench app_launch/cold_empty_workspace` exits `3`, and `scripts/run-full-perf-suite.sh --skip-main --skip-idle --fresh-reference ...` now also exits `3` after `app_launch/cold_empty_workspace` itself immediately reports the same environment blocker, the script skips the remaining launch cases, and `perf_budget_report` ends with `Skipped 962 budget(s)` plus `No fresh benchmark data matched the requested report inputs; all tracked budgets were skipped.` All six launch timing, allocation-schema, and `repos_loaded` rows stay `SKIP` because that invocation produced no fresh artifacts.
- A March 25 verification of the default launch-only script path without an explicit freshness override, `scripts/run-full-perf-suite.sh --skip-main --skip-idle`, also behaves correctly after the cold-baseline fix and report fail-closed changes: it auto-creates a suite-start `tmp/perf-suite-start.*.stamp`, `app_launch/cold_empty_workspace` immediately reports the same environment blocker, the script skips the remaining launch cases, keeps every tracked `app_launch/*` timing/allocation/`repos_loaded` row at `SKIP skipped (not run)`, ends with `Skipped 962 budget(s)` plus `No fresh benchmark data matched the requested report inputs; all tracked budgets were skipped.`, and then returns exit `3`. The default script path is therefore still safe against accidentally reusing the stale March 24 launch sidecars too.
- A March 25 verification of the optional repo-side launch-root override, `scripts/run-full-perf-suite.sh --skip-main --skip-idle --criterion-root criterion`, behaves the same way on this blocked runner: it auto-creates a suite-start freshness stamp, `app_launch/cold_empty_workspace` immediately reports the same environment blocker, the report runs with `--criterion-root criterion --criterion-root target/criterion`, ends with `Skipped 962 budget(s)` plus `No fresh benchmark data matched the requested report inputs; all tracked budgets were skipped.`, and then returns exit `3`. The `--criterion-root criterion` path is therefore also safe against stale launch-sidecar reuse under this blocker, but it still cannot produce any fresh `app_launch/*` artifacts inside this sandbox.
- A March 25 readiness verification confirms the remaining blocker is the runner environment, not the modified harness code on this worktree: `cargo test -q -p gitcomet --bin perf-app-launch` (`33` tests), `cargo test -q -p gitcomet-ui-gpui --bin perf_budget_report` (`109` tests), and `cargo bench -q -p gitcomet-ui-gpui --features benchmarks --bench performance --no-run` all pass locally. A fresh `scripts/run-full-perf-suite.sh --skip-main --skip-idle` pass now still auto-generates a new suite-start freshness stamp, skips the remaining launch cases after `app_launch/cold_empty_workspace` reports the same environment blocker, ends with `Skipped 962 budget(s)` plus `No fresh benchmark data matched the requested report inputs; all tracked budgets were skipped.`, and then returns exit `3` so that incomplete launch-only rerun cannot be mistaken for success.
- New sidecars now auto-record a top-level `.runner` object with `runner_class`, `hostname`, `os`, `arch`, and `cpu_count` fields. If you have to leave this sandbox for a compositor-capable session, set `GITCOMET_PERF_RUNNER_CLASS` to a stable label before any measured rerun so the imported `app_launch/*` artifacts can be compared mechanically to the March 25 baseline context.
- The fresh March 25 main Criterion + `idle/*` baselines already collected on this checkout came from `hostname=linuxdesktop`, `os=linux`, `arch=x86_64`, `cpu_count=32`. If an external launch rerun happens before those sections are rerun with the new `.runner` metadata, treat that fingerprint as the current baseline anchor when deciding whether the launch rerun stayed on the same runner class.
- To finish the rebaseline honestly, rerun `app_launch/*` on a compositor-capable local session of the same runner class that also permits local Unix-socket IPC to Wayland/X11/DBus, not from this sandboxed session. If the only available compositor-capable option is a different runner class, rerun the main Criterion suite and `idle/*` there too before marking the overall rebaseline complete.

## External Launch Completion Recipe (March 25, 2026)

1. On the compositor-capable runner or session, keep the runner class aligned with the fresh main Criterion + `idle/*` baselines if at all possible, and if you may need to move between sessions or machines export a stable runner label before the first measured run. New sidecars now persist that label plus the local host fingerprint under `.runner`. Then create a suite-start freshness stamp before the first launch rerun:

   ```bash
   export GITCOMET_PERF_RUNNER_CLASS="set-this-to-a-stable-runner-class-label" # optional but recommended
   fresh_reference="$(mktemp /home/sampo/git/GitComet3/tmp/perf-suite-start.external.XXXXXX.stamp)"
   ```

2. If you need to diagnose a new runner before spending time on all six cases, you may verify that it can produce allocation-aware startup milestones with:

   ```bash
   cargo run --quiet -p gitcomet --bin perf-app-launch -- --preflight-only --bench app_launch/cold_empty_workspace
   ```

   This command intentionally uses the default `gitcomet` feature set. That default already includes `ui-gpui` and therefore `ui-gpui-runtime`, so add an explicit `--features ui-gpui-runtime` only if you are overriding defaults. Because the probe reaches `first_interactive`, do not run it immediately before authoritative `cold_*` measurements on the same runner/session.

3. For the authoritative rerun, execute the launch suite directly with the freshness reference. This launch-only script path is the shortest end-to-end way to finish the remaining checklist item on that runner: it now starts with the measured `app_launch/cold_empty_workspace` case itself, skips the remaining launch cases only if a measured case exits `3` as the same environment blocker, and verifies that all six launch sidecars under the active Criterion root are at least as new as the freshness stamp, have the expected `.bench` labels, and still contain the required numeric launch timing + allocation fields before it runs the freshness-gated report:

   ```bash
   ./scripts/run-full-perf-suite.sh --skip-main --skip-idle --fresh-reference "$fresh_reference"
   ```

   That script currently uses `target/criterion` as its default active Criterion root for sidecar writes and `app_launch/*` freshness verification. If you want the rerun to refresh the checked-in repo-side artifacts under `criterion/`, add `--criterion-root criterion` here and keep that same root in the manual audit loop. The script's `perf_budget_report` step now searches the selected root first and then falls back to `target/criterion` and `criterion`, so repo-side launch sidecars can still be reported alongside fresh main-suite timings that remain under `target/criterion`. If you pass `--fresh-reference`, make sure it points to the real stamp file you created in step 1; the script now fails immediately when that path does not exist.

4. Before checking off `Run app_launch/*`, confirm that the script exited `0`; a blocked launch-only run now still reaches `perf_budget_report`, but it returns `3` afterward so the incomplete rerun cannot be mistaken for success. If you used the optional diagnostic preflight from step 2 on the same runner/session, restart or reprovision that session before treating the step 3 `cold_*` measurements as authoritative. If you bypass the script and run the six launch cases manually, then manually confirm all six `app_launch/*` sidecars under the same Criterion root are at least as new as `"$fresh_reference"`, each one includes non-missing `first_paint_ms`, `first_interactive_ms`, `first_paint_alloc_ops`, `first_paint_alloc_bytes`, `first_interactive_alloc_ops`, `first_interactive_alloc_bytes`, and `repos_loaded`, and the `.runner` fingerprint still matches the baseline runner class you intend to compare against.

   A copy-paste audit loop for that manual check:

   ```bash
   criterion_root="target/criterion" # script default; use criterion only if you passed --criterion-root criterion
   for f in "$criterion_root"/app_launch/*/new/sidecar.json; do
     fresh=true
     [[ "$f" -ot "$fresh_reference" ]] && fresh=false
     jq -r --arg file "$f" --arg fresh "$fresh" '
       [
         .bench,
         $file,
         $fresh,
         (.runner.runner_class // "unset"),
         (.runner.hostname // "missing"),
         (.runner.os // "missing"),
         (.runner.arch // "missing"),
         (.runner.cpu_count // "missing"),
         (.metrics.first_paint_ms // "missing"),
         (.metrics.first_interactive_ms // "missing"),
         (.metrics.first_paint_alloc_ops // "missing"),
         (.metrics.first_paint_alloc_bytes // "missing"),
         (.metrics.first_interactive_alloc_ops // "missing"),
         (.metrics.first_interactive_alloc_bytes // "missing"),
         (.metrics.repos_loaded // "missing")
       ] | @tsv
     ' "$f"
   done
   ```

   Every row must show `true` in the freshness column (meaning the sidecar is not older than `"$fresh_reference"`), no `missing` timing/allocation fields, and a `.runner` fingerprint that still matches the runner class you are treating as authoritative before this checklist item is marked complete.

5. Only after that external rerun should this checklist item be marked complete and the next-phase optimization candidates be appended. If that rerun happened on a different runner class than the fresh March 25 main Criterion + `idle/*` baselines already recorded here, do not treat the overall rebaseline as complete yet; rerun those other measured sections on that same runner class first so the final timing and allocation baseline stays comparable.

## Recorded Timing Outliers (March 25, 2026)

- A targeted March 25 rerun refreshed the `text_model_fragmented_edits/*` timing/allocation artifacts on the current worktree. `piece_table_edits`, `materialize_after_edits`, and `shared_string_after_edits/64` now measure `30.069 ms`, `30.858 ms`, and `30.607 ms` mean-upper bounds against a `5.000 ms` budget (`6.01x` to `6.17x`), with stable inputs (`edit_count=500`, `final_bytes=524604`); the freshness-gated report now alerts on all three again instead of skipping them.
- `idle/background_refresh_cost_per_cycle` is the biggest fresh idle timing outlier: `avg_refresh_cycle_ms=95.746` vs `50.000 ms` (`1.91x`) and `max_refresh_cycle_ms=167.048` vs `100.000 ms` (`1.67x`), with `status_ms=957.260` across `10` refresh cycles in the refreshed March 25 sidecar.
- A targeted March 25 rerun on the current worktree superseded the earlier `large_html_syntax` timing alert: `visible_window_pending/160`, `visible_window_steady/160`, and `visible_window_sweep/160` now measure `60.428 us`, `73.585 us`, and `74.471 us` mean-upper bounds, all within their `150/125/150 us` budgets, while the structural cache metrics still stayed clean (`window_lines=160`, `cache_document_present=1`, `pending=0`, `cache_hits>=160`, `cache_misses<=49`).
- A targeted March 25 rerun refreshed the `markdown_preview*` timing/allocation artifacts on the current worktree. `markdown_preview_parse_build/single_document/medium` still only grazes its budget at `2.094 ms` vs `2.000 ms`, while `markdown_preview_render_single/window_rows/200` (`333.512 us` vs `1.500 ms`), `markdown_preview_render_diff/window_rows/200` (`715.305 us` vs `2.500 ms`), and `diff_open_markdown_preview_first_window/200` (`702.987 us` vs `20.000 ms`) are fresh and within budget.
- A targeted March 25 rerun refreshed `diff_open_svg_dual_path_first_window/200` on the current worktree. It now measures `26.691 ms` mean-upper bound against a `25.000 ms` budget (`1.07x`), with structural metrics still clean (`rasterize_success=1`, `fallback_triggered=1`, `images_rendered=1`, `divider_count=1`) and allocation counters still at `0 B` / `0` ops, so this is a confirmed timing-only graze rather than an allocation candidate.
- `repo_switch/*` `effect_count` alerts stay excluded from timing ranking because they are structural, and `app_launch/*` is still blocked on this runner's missing usable compositor/display session.

## Recorded Allocation Outliers (March 25, 2026)

- `markdown_preview_parse_build/two_sided_diff/large` is the clearest fresh allocation-churn outlier at `1,120,046,036 B` across `135,362` alloc ops; the same benchmark family still allocates `151,277,598 B` for `two_sided_diff/medium`, versus `7,962,330 B` and `2,988,224 B` for the `single_document/{large,medium}` peers (`140.67x` and `50.62x` higher on like-for-like family inputs).
- `search/file_diff_ctrl_f_open_and_type_100k_lines` dominates fresh `search/*` allocation churn at `470,933,593 B`, `4,215,930` alloc ops, and `2,699,936 B` net; the next nonzero fresh search sidecar is only `114,944 B` (`search/in_diff_text_search_incremental_refinement`), so this path is the first search allocation candidate.
- `idle/background_refresh_cost_per_cycle` is the main fresh idle allocation outlier at `339,901,155 B`, `719,888` alloc ops, and `1,188,792 B` net across `10` refresh cycles (`33,990,115.5 B` per cycle); that is `6.33x` more allocated bytes and `9.08x` more alloc ops than `idle/wake_from_sleep_resume`.
- The refreshed `text_model_fragmented_edits/*` slice stays clustered at `92,040,124 B` for `piece_table_edits` and `93,089,352 B` for both `materialize_after_edits` and `shared_string_after_edits/64`, versus `1,048,590 B` for `text_model_fragmented_edits/string_edit_control` (`87.78x` to `88.78x` higher).
- `conflict_load_duplication/duplicated_text_and_bytes/low_density` and `conflict_load_duplication/duplicated_text_and_bytes/high_density` remain the clearest duplication-specific allocation regressions at `56,529,548 B` and `56,479,766 B`, while the paired `shared_payload_forwarding/{low_density,high_density}` cases stay at `28 B`.
- By retained bytes rather than total churn, the current fresh leaders are `diff_open_markdown_preview_first_window/200` (`6,793,344 B` net), `markdown_preview_render_diff/window_rows/200` (`5,732,864 B` net), and `markdown_preview_render_single/window_rows/200` (`3,930,376 B` net); `app_launch/*` remains excluded here because this runner still could not refresh those sidecars.

## New Benchmark Coverage (March 27, 2026)

- Added `markdown_preview_scroll/window_rows/200` to track steady-state Preview-mode scrolling over the existing large single-document markdown preview fixture.
- Added `markdown_preview_scroll/rich_5000_rows_window_rows/200` to track steady-state scrolling over a rendered 5,000-row markdown preview document with `500` long rows at roughly `2,000` characters each, plus headings, lists, tables, blockquotes, details summaries, paragraphs, and code rows mixed throughout.
- The rich `markdown_preview_scroll/rich_5000_rows_window_rows/200` fixture is built directly as rendered preview rows so it can model that document shape without tripping the production single-file `1 MiB` markdown preview source limit.
- Both new markdown scroll cases are wired into `perf_budget_report` and the auto-synced benchmark checklist below, but they still need their first fresh timing/allocation baselines on this worktree.

<!-- BEGIN AUTO BENCHMARK TASKS -->
## Benchmark Optimization Tasks

This section is auto-synced from the current local performance suite by `./local_agent_loop.sh`.
Every benchmark gets its own task. Do not check off a task until the before/after documentation includes CPU timing plus allocation bytes and allocation ops for the same benchmark.

- [ ] `open_repo/balanced` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `open_repo/history_heavy` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `open_repo/branch_heavy` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `open_repo/extreme_metadata_fanout` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `branch_sidebar/local_heavy` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `branch_sidebar/remote_fanout` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `branch_sidebar/aux_lists_heavy` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `branch_sidebar/20k_branches_100_remotes` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `branch_sidebar/cache_hit_balanced` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `branch_sidebar/cache_miss_remote_fanout` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `branch_sidebar/cache_invalidation_single_ref_change` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `branch_sidebar/cache_invalidation_worktrees_ready` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `history_graph/linear_history` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `history_graph/merge_dense` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `history_graph/branch_heads_dense` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `history_cache_build/balanced` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `history_cache_build/merge_dense` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `history_cache_build/decorated_refs_heavy` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `history_cache_build/stash_heavy` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `history_cache_build/50k_commits_2k_refs_200_stashes` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `history_load_more_append/page_500` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `history_scope_switch/current_branch_to_all_refs` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `repo_switch/refocus_same_repo` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `repo_switch/two_hot_repos` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `repo_switch/selected_commit_and_details` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `repo_switch/twenty_tabs` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `repo_switch/20_repos_all_hot` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `repo_switch/selected_diff_file` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `repo_switch/selected_conflict_target` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `repo_switch/merge_active_with_draft_restore` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `commit_details/many_files`  -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `commit_details/deep_paths` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `commit_details/huge_file_list` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `commit_details/large_message_body` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `commit_details/10k_files_depth_12` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `commit_details/select_commit_replace` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `commit_details/path_display_cache_churn` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `status_list/unstaged_large` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `status_list/staged_large` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `status_list/20k_entries_mixed_depth` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `status_multi_select/range_select` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `status_select_diff_open/unstaged` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `status_select_diff_open/staged` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `merge_open_bootstrap/small` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `merge_open_bootstrap/large_streamed` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `merge_open_bootstrap/many_conflicts` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `merge_open_bootstrap/50k_lines_500_conflicts_streamed` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `frame_timing/continuous_scroll_history_list` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `frame_timing/continuous_scroll_large_diff` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `frame_timing/sidebar_resize_drag_sustained` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `frame_timing/rapid_commit_selection_changes` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `frame_timing/repo_switch_during_scroll` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `keyboard/arrow_scroll_history_sustained_repeat` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `keyboard/arrow_scroll_diff_sustained_repeat` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `keyboard/tab_focus_cycle_all_panes` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `keyboard/stage_unstage_toggle_rapid` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `staging/stage_all_10k_files` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `staging/unstage_all_10k_files` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `staging/stage_unstage_interleaved_1k_files` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `undo_redo/conflict_resolution_deep_stack` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `undo_redo/conflict_resolution_undo_replay_50_steps` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `git_ops/status_dirty_500_files` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `git_ops/log_walk_10k_commits` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `git_ops/log_walk_100k_commits_shallow` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `git_ops/status_clean_10k_files` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `git_ops/ref_enumerate_10k_refs` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `git_ops/diff_rename_heavy` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `git_ops/diff_binary_heavy` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `git_ops/diff_large_single_file_100k_lines` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `git_ops/blame_large_file` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `git_ops/file_history_first_page_sparse_100k_commits` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `diff_scroll/normal_lines_window/200` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `diff_scroll/long_lines_window/200` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `file_diff_replacement_alignment/balanced_blocks/scratch` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `file_diff_replacement_alignment/balanced_blocks/strsim` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `file_diff_replacement_alignment/skewed_blocks/scratch` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `file_diff_replacement_alignment/skewed_blocks/strsim` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `text_input_prepaint_windowed/window_rows/80` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `text_input_prepaint_windowed/full_document_control` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `text_input_runs_streamed_highlight_dense/legacy_scan` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `text_input_runs_streamed_highlight_dense/streamed_cursor` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `text_input_runs_streamed_highlight_sparse/legacy_scan` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `text_input_runs_streamed_highlight_sparse/streamed_cursor` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `text_input_long_line_cap/capped_bytes/4096` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `text_input_long_line_cap/uncapped_control` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `text_input_wrap_incremental_tabs/full_recompute` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `text_input_wrap_incremental_tabs/incremental_patch` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `text_input_wrap_incremental_burst_edits/full_recompute/12` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `text_input_wrap_incremental_burst_edits/incremental_patch/12` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `text_model_snapshot_clone_cost/piece_table_snapshot_clone/8192` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `text_model_snapshot_clone_cost/shared_string_clone_control/8192` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `text_model_bulk_load_large/piece_table_append_large` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `text_model_bulk_load_large/piece_table_from_large_text` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `text_model_bulk_load_large/string_push_control` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `text_model_fragmented_edits/piece_table_edits` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `text_model_fragmented_edits/materialize_after_edits` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `text_model_fragmented_edits/shared_string_after_edits/64` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `text_model_fragmented_edits/string_edit_control` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `file_diff_syntax_prepare/file_diff_syntax_prepare_cold` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `file_diff_syntax_prepare/file_diff_syntax_prepare_warm` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `file_diff_syntax_query_stress/nested_long_lines_cold` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `file_diff_syntax_reparse/file_diff_syntax_reparse_small_edit` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `file_diff_syntax_reparse/file_diff_syntax_reparse_large_edit` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `file_diff_inline_syntax_projection/visible_window_pending/200` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `file_diff_inline_syntax_projection/visible_window_ready/200` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `file_diff_syntax_cache_drop/deferred_drop/4` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `file_diff_syntax_cache_drop/inline_drop_control/4` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `prepared_syntax_multidoc_cache_hit_rate/hot_docs/6` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `prepared_syntax_chunk_miss_cost/chunk_miss` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `large_html_syntax/synthetic_html_fixture/background_prepare` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `large_html_syntax/synthetic_html_fixture/visible_window_pending` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `large_html_syntax/synthetic_html_fixture/visible_window_steady` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `large_html_syntax/synthetic_html_fixture/visible_window_sweep` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `worktree_preview_render/cached_lookup_window/200` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `worktree_preview_render/render_time_prepare_window/200` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `markdown_preview_parse_build/single_document/medium` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `markdown_preview_parse_build/two_sided_diff/medium` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `markdown_preview_parse_build/single_document/large` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `markdown_preview_parse_build/two_sided_diff/large` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `markdown_preview_render_single/window_rows/200` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `markdown_preview_render_diff/window_rows/200` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `markdown_preview_scroll/window_rows/200` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `markdown_preview_scroll/rich_5000_rows_window_rows/200` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `diff_open_markdown_preview_first_window/200` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `diff_open_image_preview_first_paint` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `diff_open_svg_dual_path_first_window/200` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_three_way_scroll/style_window/200` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_three_way_prepared_syntax_scroll/style_window/200` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_three_way_visible_map_build/linear_two_pointer` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_three_way_visible_map_build/legacy_find_scan` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_two_way_split_scroll/window_100` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_two_way_split_scroll/window_200` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_two_way_split_scroll/window_400` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_load_duplication/shared_payload_forwarding/low_density` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_load_duplication/duplicated_text_and_bytes/low_density` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_load_duplication/shared_payload_forwarding/high_density` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_load_duplication/duplicated_text_and_bytes/high_density` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_two_way_diff_build/full_file/low_density` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_two_way_diff_build/block_local/low_density` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_two_way_diff_build/full_file/high_density` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_two_way_diff_build/block_local/high_density` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_two_way_word_highlights/full_file/low_density` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_two_way_word_highlights/block_local/low_density` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_two_way_word_highlights/full_file/high_density` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_two_way_word_highlights/block_local/high_density` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_resolved_output_gutter_scroll/window_100` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_resolved_output_gutter_scroll/window_200` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_resolved_output_gutter_scroll/window_400` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_search_query_update/window/200` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `patch_diff_search_query_update/window_200` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `patch_diff_paged_rows/eager_full_materialize` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `patch_diff_paged_rows/paged_first_window/200` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `patch_diff_paged_rows/inline_visible_eager_scan` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `patch_diff_paged_rows/inline_visible_hidden_map` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `diff_open_patch_first_window/200` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `diff_open_file_split_first_window/200` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `diff_open_file_inline_first_window/200` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `diff_open_patch_deep_window_90pct/200` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `diff_open_patch_100k_lines_first_window/200` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `diff_open_conflict_compare_first_window/200` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `diff_refresh_rev_only_same_content/rekey` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `diff_refresh_rev_only_same_content/rebuild` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_split_resize_step/window/200` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_streamed_provider/index_build` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_streamed_provider/projection_build` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_streamed_provider/first_page/200` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_streamed_provider/first_page_cache_hit/200` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_streamed_provider/deep_scroll_50pct/200` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_streamed_provider/deep_scroll_90pct/200` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_streamed_provider/search_rare_text` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_streamed_provider/search_common_text` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_streamed_resolved_output/projection_build` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_streamed_resolved_output/window/200` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `conflict_streamed_resolved_output/deep_window_90pct/200` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `resolved_output_recompute_incremental/full_recompute` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `resolved_output_recompute_incremental/incremental_recompute` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `pane_resize_drag_step/sidebar` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `pane_resize_drag_step/details` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `diff_split_resize_drag_step/window_200` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `window_resize_layout/sidebar_main_details` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `window_resize_layout/history_50k_commits_diff_20k_lines` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `history_column_resize_drag_step/branch` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `history_column_resize_drag_step/graph` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `history_column_resize_drag_step/author` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `history_column_resize_drag_step/date` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `history_column_resize_drag_step/sha` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `repo_tab_drag/hit_test/20_tabs` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `repo_tab_drag/reorder_reduce/20_tabs` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `repo_tab_drag/hit_test/200_tabs` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `repo_tab_drag/reorder_reduce/200_tabs` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `scrollbar_drag_step/window_200` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `search/commit_filter_by_author_50k_commits` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `search/commit_filter_by_message_50k_commits` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `search/in_diff_text_search_100k_lines` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `search/in_diff_text_search_incremental_refinement` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `search/file_preview_text_search_100k_lines` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `search/file_diff_ctrl_f_open_and_type_100k_lines` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `search/file_fuzzy_find_100k_files` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `search/file_fuzzy_find_incremental_keystroke` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `fs_event/single_file_save_to_status_update` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `fs_event/git_checkout_200_files_to_status_update` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `fs_event/rapid_saves_debounce_coalesce` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `fs_event/false_positive_rate_under_churn` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `network/ui_responsiveness_during_fetch` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `network/progress_bar_update_render_cost` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `network/cancel_operation_latency` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `clipboard/copy_10k_lines_from_diff` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `clipboard/paste_large_text_into_commit_message` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `clipboard/select_range_5k_lines_in_diff` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `display/render_cost_1x_vs_2x_vs_3x_scale` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `display/two_windows_same_repo` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `display/window_move_between_dpis` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `real_repo/monorepo_open_and_history_load` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `real_repo/deep_history_open_and_scroll` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `real_repo/mid_merge_conflict_list_and_open` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `real_repo/large_file_diff_open` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `idle/cpu_usage_single_repo_60s` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `idle/cpu_usage_ten_repos_60s` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `idle/memory_growth_single_repo_10min` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `idle/memory_growth_ten_repos_10min` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `idle/background_refresh_cost_per_cycle` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `idle/wake_from_sleep_resume` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `app_launch/cold_empty_workspace` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `app_launch/cold_single_repo` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `app_launch/cold_five_repos` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `app_launch/cold_twenty_repos` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `app_launch/warm_single_repo` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
- [ ] `app_launch/warm_twenty_repos` -- baseline CPU: pending; baseline alloc bytes: pending; baseline alloc ops: pending; post CPU: pending; post alloc bytes: pending; post alloc ops: pending; notes:
<!-- END AUTO BENCHMARK TASKS -->
